# 共同校准最简人工工作流决策

## Material Passport

- Origin Skill: academic-research-suite / experiment-agent
- Origin Date: 2026-08-15
- Document Status: `CURRENT_ALIGNED`
- Decision Status: `APPROVED`
- Internal Version: `v1.6`
- Canonical Artifact: `docs/data-dictionary/编码表.md`
- Canonical Version at Decision: `v3.6.1`
- Current Alignment Snapshot: `v3.14.0`

## 一、需要裁决的问题

20—30篇共同校准的目的，是用真实材料发现预定义类目的边界问题并修订编码簿，不是生产正式信度或主题分析结果。若要求两名编码员同时维护主编码、独立问题表、JSON证据、文本offset和修订决策，会重复记录同一疑难判断，显著增加两人团队负担，并可能使编码员把注意力从内容判断转移到审计格式。

## 二、最终决定

1. 编码员只填写一张共同校准主表，不填写独立问题登记表或修订决策表。
2. 任务开始前由准备者冻结样本编号、观察单位、材料、字段、值域、编码表版本和模板版本；进入人工编码后不得由系统动态提示、补值、派生或纠错。
3. 编码员只填写编码值和逐字段置信度；仅在`confidence <= 2`或`UNRESOLVED`时选择原因并写一句简短说明。没有合理替代值时不强迫枚举替代编码。
4. 实质性阳性文本判断只复制最短充分证据原文；人工金标锁定后，离线转换工具才可计算Unicode字符offset。同一引文重复出现时才要求填写起始位置。
5. 框架遗漏、反例、语境依赖或切分问题只在主表的条件字段中记录一次，不重复抄写到负责人表。
6. 双人编码、信度检验和人工裁决全部结束并锁定金标后，确定性离线转换工具才可生成规范`labels.csv`中的JSON和offset；该工具不得反向修改任何人工字段。
7. 问题分类、合并、关闭、版本升级和重编码范围由两名研究负责人讨论后写入`calibration-issues.csv`和`revision-decisions.csv`；普通编码员不承担这些工作。

## 三、人工负担边界

| 角色 | 必须完成 | 不承担 |
|------|----------|--------|
| 编码员 | 编码值、逐字段置信度、条件备注、最短证据原文、极少数额外问题 | JSON、offset计算、问题编号、问题合并、版本升级、重编码范围 |
| 转换工具（仅金标锁定后） | 记录校验、JSON、唯一offset与规范训练格式 | 类目决定、问题裁决、编码簿修订、人工金标回写 |
| 研究负责人 | 复核低置信/未决项、正式问题分类、修订决定与重编码范围 | 替代编码员完成原子字段判断 |

## 四、阶段门

- 本决定和空白模板足以启动20—30篇共同校准；不要求先建立数据库schema。
- `UNRESOLVED`必须在独立盲试标前关闭。
- `at_eval_subtype`的单选/多选责任以及共同校准后确有必要的字段专门`UNK/NA`规则，仍须在独立盲试标前冻结。
- V4新增`rs_r_rec`、`rs_r_evt`及四个事件子类须纳入共同校准；旧`rs_r_act`记录不能拆分迁移，受影响材料须按原文重编码。
- 共同校准材料不能进入正式信度估计；盲试标必须使用未参与规则讨论的新样本。
- v3.14.0首轮采用同轮但相互隔离的双任务包：25名作者V0与25篇作者互斥V1—V6。两任务不共享作者，V0不看目标内容结果，内容编码员不看角色与作者资料。V0的EA、CE、SC、证据充分性和最终角色全部人工填写与裁决。
- 当前任务包已经生成，但人工字段仍为空；共同校准完成、规则v0.2冻结后，另取50名新作者独立盲试标。

## 五、实现文件

- `data/annotations/templates/all-label-manual-coding.xlsx`：固定文件名的人工宽表；当前内部模板版本为`all-label-manual-coding-v2.5`。帖子级8个和图像级6个上级分类字段位于对应展开子类组之前，其中9个主观上级字段与置信度列相邻；文本V4新增6个人工字段及各自置信度列，原`rs_r_act`列移出人工模板。父子值均由编码员填写，只作复核。
- `创作者层面人工编码表.xlsx`：V0独立纯人工工作簿；含作者主表、角色锁定后使用的触达记录、中文标签示例和人工裁决表，不含公式、模型建议或自动派生。
- `data/annotations/templates/calibration-coding.csv`：编码员唯一主表契约。
- `data/annotations/templates/labels.csv`：转换后的规范原子标签契约。
- `data/annotations/templates/calibration-issues.csv`：研究负责人问题清单。
- `data/annotations/templates/revision-decisions.csv`：研究负责人修订决策。
- `scripts/annotation_prepare_calibration.py`：主表校验、规范化与问题队列提取入口。

## 六、内部迭代记录

| 内部版本 | 日期 | 变更 |
|----------|------|------|
| v1.0 | 2026-08-15 | 批准编码员单表、条件备注、自动offset和负责人后处理工作流 |
| v1.1 | 2026-08-21 | 登记编码表v3.9.0与固定Excel内部模板2.2；全部人工标签及其适用的逐字段置信度由编码员填写，原分工与审计边界不变 |
| v1.2 | 2026-08-21 | 同步固定Excel内部模板2.3的上级分类字段与相邻置信度列；共同校准分工和审计边界不变 |
| v1.3 | 2026-08-21 | 对齐编码表v3.10.0和模板2.4；将V2新增活动/事件字段及旧字段重编码要求纳入共同校准工作流 |
| v1.4 | 2026-08-22 | 对齐编码表v3.11.0和模板2.5；共同校准流程不变，现行资源维度改称V4 |
| v1.5 | 2026-08-26 | 对齐编码表v3.13.0；登记25名作者V0与25篇作者互斥V1—V6双任务包、T0/T1隔离及50名新作者盲试标门 |
| v1.6 | 2026-08-26 | 对齐编码表v3.14.0；冻结人工编码期无系统参与、金标锁定后才离线转换，以及V0组件和最终角色全部人工判断的边界 |
