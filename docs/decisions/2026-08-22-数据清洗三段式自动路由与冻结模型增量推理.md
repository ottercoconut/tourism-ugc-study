# 数据清洗三段式自动路由与冻结模型增量推理

> **状态**：`FRAMEWORK_FROZEN / REFERENCE_DEDUP_PENDING / THRESHOLD_PENDING`
>
> **稳定参考契约名**：`final-nonduplicate-model-reference`
>
> **替代**：[数据清洗通用文本模型与全局审计口径](2026-08-22-数据清洗通用文本模型与全局审计口径.md)
>
> **适用范围**：旅游相关性数据清洗；不改变 KOL/KOC、内容编码或视觉研究的方法。

## 背景

数据清洗的实际目标是学习帖子是否与青岛旅游研究相关，并把这一判断用于自动化无关内容清洗。系统既需要处理当前未标注人口，也需要在未来新增记录到来时继续清洗，而不是每个批次都重新训练。与此同时，模型置信度不足的记录必须交给人工处理，自动排除只改变派生分析发布，不得删除源数据。

现有人工工作形成的700条完成 CSV 和对应 manifest 仍是迁移输入，但其中可能存在跨身份相似内容。它们不能直接进入训练；必须先完成全对重复确认和稳定补样，冻结唯一 `final-nonduplicate-model-reference` CSV＋`finalized` manifest。旧文件及所有中间人工文件只作生成谱系，标签不复制到数据库标注表。

## 评估过的方案

1. **全量人工处理**：避免模型错误，但无法实现所需的自动清洗能力，也不利于未来新增批次持续处理。
2. **单阈值自动二分**：自动覆盖高，但会强迫边界记录作自动决定，无法为置信度不足的记录保留人工通道。
3. **三段式选择性路由**：低无关概率自动保留，中间区间人工处理，高无关概率自动排除；在自动覆盖和错误控制之间保留明确接口。

增量处理同时比较了“每个新批次重新训练”和“冻结模型只做推理”。重新训练会让不同批次的概率和决定依赖不同模型，也把新增数据处理与重新标注、重新评估绑定；冻结模型推理能让当前与未来批次共享同一决策基础。

人工标签存储比较了“复制进数据库标注表”和“完成 CSV + manifest 作为权威源”。复制会产生两个可能不一致的标签副本；直接引用冻结文件并在数据库保存最终决定及证据哈希，权威关系更清楚。

## 最终决定

1. 所有来源帖子共同训练一个通用文本模型，输入只有规范化文本。平台只保留为来源谱系和构成披露字段，不进入特征、切分、阈值、审计配额或分类性能报告。
2. 首个正式模型采用字符 TF-IDF 和线性 SVM。训练部分内部以泄漏分组交叉验证生成折外分数，再用 Sigmoid 方法校准为 `p_unrelated`。
3. 作者、精确重复和最终确认的近重复关系共同组成泄漏分组。全体参考集统一按时间留出 20% 测试候选，再扩展到完整泄漏分组；其余数据进行全局分组分层训练/验证切分。
4. 模型与校准器冻结为不可变 artifact；阈值策略单独冻结。调整阈值不重新训练模型。
5. 路由固定为：
   - `p_unrelated <= T_keep`：自动保留；
   - `T_keep < p_unrelated < T_exclude`：人工处理；
   - `p_unrelated >= T_exclude`：从派生分析发布中自动排除，不删除源数据。
6. 唯一权威标签证据是恰好700条、身份唯一、标签二元且无最终确认重复成员的 `final-nonduplicate-model-reference` CSV，以及与其唯一配对的 `finalized` manifest。训练拒绝旧完成 CSV 和中间文件；推理跳过最终700条，标签不复制到 `text_post_annotations`。
7. 最终仍保持500条 `probability` 和200条 `targeted`。补样使用读取标签前冻结的全局队列，不设平台配额或排序。概率框替换不得继承旧概率或权重；无法证明冻结补样设计的总体估计有效性时明确标记不可用。
8. 初始全量和未来新增批次使用同一个冻结模型。新增记录通过结构门和规范化后直接推理，不触发 `fit` 或 artifact 替换。
9. 人工灰区、未来新增人工结果和保留集审计继续使用各自独立的完成 CSV 与 manifest。数据库只保存最终帖子决定和相应 CSV 哈希、任务身份、模型、策略及推理运行引用。
10. 部署后的常规审计只针对最终保留人口，采用全局抽样且不设置平台配额。自动排除人口不做常规随机审计。

## 当前未冻结项目

以下项目全部为 `UNSET`：

- `T_keep` 与 `T_exclude`；
- 保留集审计事件定义、样本量、最大事件率、单侧置信上限和区间方法；
- 模型测试指标和自动覆盖率的最低接受条件；
- 保留集审计失败后的恢复条件。

两个阈值最终必须满足 `0 <= T_keep < T_exclude <= 1`。在阈值、测试门和审计门冻结前，系统可以生成研究性概率，但所有未有人工作答的记录只能转人工，不得自动保留或自动排除。

## 失败降级与恢复

保留集审计失败时，模型仍可继续打分，但自动保留暂停，原自动保留区间全部转入人工处理。自动排除和原人工灰区继续按冻结策略运行。恢复自动保留必须创建并绑定新的阈值策略，满足尚待冻结的恢复门；不得原地修改旧策略、改写旧运行或更换抽样结果以追求通过。

参考 CSV/manifest 哈希不符、记录身份或规范化文本不一致、泄漏跨集合、测试集参与调参、阈值非法、推理调用训练、源库写入或 artifact 不完整时，运行必须失败关闭。

## 可靠性声明边界

正式结果可以声明的是：在冻结测试证据、三段式路由和通过门的保留集审计共同约束下，最终保留语料的残留无关风险受到控制。

由于不对自动排除人口做常规随机审计，不能声称部署期所有被排除记录都经过直接验证。避免过度删除的证据仅来自锁定测试集上的总体 Precision、Recall、F1、PR-AUC、混淆矩阵和阈值表现；论文必须披露这一限制。

## 实施状态与正式化

本决策继续冻结文档和工程接口。最终参考集的全对候选、人工决定、候补调度、不可变 artifact、只读验证，以及字符 TF-IDF＋线性 SVM＋训练集分组折外 Sigmoid baseline 代码已经完成。实际700条的初始近重复人工确认已完成并封存，候补重复与补充标签仍未关闭；首次正式模型未训练，锁定测试集未开启，也未执行推理或确定阈值。

首次候选暴露出48条 Delta JSON 被旧规则当作普通正文，其中结构键和属性抬高了相似度，且先做字符替换还破坏了部分 JSON。该轮113条候选及人工文件保留为旧谱系，不再继续作答。修订后从候选构建绑定的只读冻结源快照重做文本投影，只拼接 Delta 字符串 `insert`，忽略格式属性和已声明的非文本嵌入；异常结构失败关闭。训练入口改为读取最终 CSV 中已验证的规范化文本，不再取旧派生库文本。

修订后再次完整比较244,650对，生成45条阈值候选、0条精确规范哈希重复边，作业文本不存在 Delta 结构残留。待处理 CSV SHA-256 为 `03a2e81dd676ae002ea815e706ffd8f49b54e2afdddbebf36840f878c0aa79ad`，配对 pending manifest SHA-256 为 `713fa419f240a01a00c9f91e4b56d46eb5ee28b8bc74966c8e07a3ca364b19d2`。旧53条非空判断已按无序稳定帖子对迁移：6条两端正文未变化的 `not_duplicate` 继续 finalized；9条旧 `duplicate` 虽以低于0.80的显式人工边和预填决定保留，但两端正文均因结构提取而变化，故本轮状态恢复为 pending，必须基于新正文重新确认；39条新自动候选也待判断，共48条待处理。其余38条旧 `not_duplicate` 不再进入新候选，但仍保留在旧谱系。迁移 manifest 绑定旧完成 CSV、旧 pending manifest、新 pending CSV/manifest、迁移种子副本及6/9/38分类摘要；私有人工文件不提交 Git，也不是权威标签证据。

人工完成后，封存结果为54条最终决定：45条 `duplicate`、9条 `not_duplicate`；完成 CSV SHA-256 为 `292e8dcdd9f9552bfe308aca17c188eaa9aa83322b552efa6d62c54802b503e1`，配对 finalized manifest SHA-256 为 `bce4686e766951693fe183abf0d3973ef55d318c06f2a7be49d8575023fd0e93`。确认边形成673个稳定代表，排除27个重复成员，剩余 probability 488条、targeted 185条，缺口分别为12和15；组内旅游标签冲突为0。确定性全局候补队列含13,158条，首批冻结前缀取27条，只产生1条新的近重复候选，人工已确认为 `duplicate`。为补足被排除的1条，冻结前缀按原顺序扩展至第28名；重算没有新增候选，原判断因两端身份与正文不变而沿用，配对 finalized manifest SHA-256 为 `36f83915bf95cbef3f35719ba43beb119882f63bb1130c5e3924325da7bf9020`。候补标签冲突仍为0，现生成27条补充旅游相关性任务，pending CSV SHA-256 为 `e74b57f227144e984796d9f2d95d6880993a046217d7ce07828d654b317996c2`，配对 pending manifest SHA-256 为 `28c8e20a1954c6bc8a23a0aa3a7f4cbfd2b241dbf7abb7934895b723f6b330d3`。私有目录根部只保留当前可操作 CSV，已封存证据、冻结输入和压缩谱系按状态隔离，避免并列误用。

后续完成27条候补旅游相关性标注，封存唯一最终700条；随后生成包含同作者边的 leakage build、执行首次 baseline 训练，再依据开发证据讨论全部 `UNSET` 项。禁止在最终参考集 finalized 前生成正式 leakage build 或训练。四份核心文档只有在阈值、测试和验收全部完成后才能统一改为 `CURRENT / FORMAL`。
