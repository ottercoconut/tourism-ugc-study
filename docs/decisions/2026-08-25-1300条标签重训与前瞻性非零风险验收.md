# 1300条标签重训与前瞻性非零风险验收

> **文档状态**：`CURRENT CLEANING DECISION`
> **内部文档版本**：`v1.1`
> **编码表对齐边界**：本文中的“标签”仅指旅游相关性清洗标签，不是V0—V11内容编码；已按编码表v3.13.0审阅边界，无语义依赖。

## 决策

Issue #49 启动一次有限、收口的模型重训周期。最终700条、Wave A 240条和
Wave B 360条共1,300条确定人工标签全部进入新训练池。旧 Qwen 模型
`e24fc7a65a5e0e52f383725e21607837` 的锁定测试结论继续是
`FAILED_MANUAL_ONLY`；原148条测试已经消费，虽然可进入新训练，但必须标为
`historical_test_consumed`，不得再次作为测试或独立评价证据。

旧协议要求自动尾部零方向性错误，这一门适合当时的保守放行决策，但对存在主观
边界的文本分类过严。新协议只对新训练运行前瞻生效，不事后改判旧模型。新候选
必须同时满足：自动排除端的 `related/uncertain` 不高于2%，自动保留端的
`unrelated/uncertain` 不高于5%。区间必须完整披露，但点估计满足门槛即可进入
候选选择；不再要求观察错误数为零或置信上界低于门槛。

## 固定训练与选择边界

1. 训练快照绑定来源、规范正文哈希、源身份和 finalized leakage component；同组
   不得跨折。Wave A/B 设计权重只用于交叉拟合后的总体评价，绝不进入 `fit`。
2. Qwen3-Embedding-4B 对超过内容预算的文本按连续、无遗漏的2048-token预算块
   编码；记录级向量为全部块算术平均后L2规范化，省略token必须为0。
3. 固定且仅比较 Qwen聚合向量＋LinearSVC、字符TF-IDF＋LinearSVC、两者交叉
   拟合logit的逻辑回归融合。完成后停止模型研发，不追加候选族或标注。
4. 三种候选均生成5折 leakage-group OOF，Sigmoid校准也必须交叉拟合，保证成员
   自身标签不进入自身概率。Wave B成员的OOF概率按原设计权重评价。
5. `T_keep=0.01..0.49`、`T_exclude=0.51..0.99`，步长0.01；每个自动尾部至少
   30条原始确定标签。输出完整风险—人工率Pareto前沿；风险合格者优先选择人工
   率最低，再依次比较UGC误删、无关误留、log loss、实现复杂度与稳定候选ID。
6. 历史 Qwen `0.14/0.86` 的Wave B人工率20.36%仅作参照。降低至少3个百分点
   记为明显改善；未达到也接受风险合格的最佳固定候选，不扩大搜索。

选定后使用全部1,300条拟合唯一最终模型并冻结校准器与双阈值。历史测试不重开，
本周期唯一独立部署放行证据改为冻结策略后的新双尾盲审。

## 前瞻性盲审与降级

模型和阈值冻结后，对尚未人工标注的完整人口执行纯预测，并从 `auto_keep` 与
`auto_exclude` 各简单随机无放回抽150条。任务为UTF-8 BOM四列表；人工只填写
`tourism_label`。排除端最多允许3条 `related/uncertain`，保留端最多允许7条
`unrelated/uncertain`。同时报告单侧Clopper–Pearson和按leakage component聚类
区间，但区间不作硬门。

抽样一次冻结，不得补抽、替换、追加或改阈值稀释失败。某一尾失败只把该动作
降为人工；两尾失败则全部人工；通过尾可以独立启用。1,300条训练成员直接使用
人工标签，300条审计成员也由人工标签覆盖模型动作。自动排除只写派生决定，源库
始终只读。正式推理不得调用 `fit`，并须支持checkpoint、严格复用和批次幂等。

## 可复现身份

- 冻结配置：`configs/cleaning-model-retraining.yaml`；
- 计划ID：`19893303fa975523448c12a06aa72b57`；
- 旧锁定测试运行：`3ae5ccddabf10548f28e472bd4d46695`；
- Wave B评价：`fc5c2721c4e15e8addf7c55ab115950e`；
- leakage build：`6048c9aba31e020cb2fae9afb0b676e2`。

平台、样本框、旧模型概率和抽样层不得进入特征、分组、阈值、配额、排序或验收
门。实际CSV、私有映射、正文、数据库、编码向量和模型权重均不得提交Git。

## 执行记录

- 训练快照：`6f1075ec9b70ca1485208762cb7a6453`；
- 完整Qwen编码：`0d1a5bb7eb05fca01f943ae549a11bbe`，总省略token为0；
- 固定候选训练：`b89e34ad89bdc954de7e7ed540a710c2`；
- 冻结策略：融合候选、`T_keep=0.44 / T_exclude=0.96`，策略ID
  `c4b708a22568e28f433f710973213672`；
- 人口纯预测：`a220076d957c00052090953c8306ce65`，12,558条，临时动作
  6,537/2,010/4,011，`fit_call_count=0`、源库写入数为0；
- 新双尾盲审：`2a5dda318995535400332bdf031a237e`，每尾150条，任务已一次
  冻结；完成表为139条related、161条unrelated、0条uncertain；
- 审计判读：`b618ae76cc6328f776a87d4f5db89092`。排除端0/150，通过；
  保留端11/150，失败并降为人工；
- 最终派生决定：`910b544d4dc6310751a262406ccbe3fe`。模型仅启用
  `auto_exclude`，自动排除3,861条；全体动作是exclude 4,737、keep 724、
  manual_review 8,397。

该单尾决定随后因剩余人工率68.50%被研究者明确拒绝采用。其运行与审计结论保持
不可变，但不再代表当前授权状态。当前使用既有概率做2,401组完整阈值事后探索；
探索不重新推理、不扩展模型族，也不自动冻结新policy。最后300条一旦参与阈值
比较，其角色即转为事后诊断，不能同时作为新阈值的独立放行证据。

事后探索 `8df3dcac9a2985ac53314a86629f88fc` 得到56组有原抽样范围支持且满足
点风险参照的组合。按人工率及固定风险顺序的非冻结推荐是0.15/0.96：完整人口
三段数量为5,585/2,962/4,011，人工率23.59%；Wave B两尾不利事件为4/108和
0/89，最后300条中落入新两尾的子样本为6/127和0/150。该比较利用了最后盲审
标签来选择阈值，存在多重比较与选择后评价，因此只支持研究决策，不构成独立的
部署放行。

## 研究者终局阈值与交付决定

研究者在阅读全部2,401组聚合结果后明确选择融合模型
`721a2601f8ba8622257c1067ae85385b`、`T_keep=0.31`、
`T_exclude=0.96`。这不是上述预设3/7事件门的“通过”，也不是新的独立测试：
旧审计 `ONE_TAIL_RELEASED`、保留端11/150失败和旧策略单尾降级均保持不可变。
新选择的依据固定为 `researcher_accepted_post_hoc_risk`；最后300条只作事后
阈值诊断，存在多重比较和选择后评价，不能用于无偏地证明新策略人口错误率。

0.31/0.96在12,558条既有概率上的三段数量为6,255条自动保留、2,292条人工
中间层、4,011条自动排除，人口人工率18.25%。Wave B OOF保留端为14/141原始
不利事件（9.93%），设计加权风险3.28%，单侧95% Clopper–Pearson上界15.09%；
排除端为0/89，设计加权风险0%，单侧上界3.31%。最后300条在新阈值支持范围内
的保留端为9/144（6.25%，单侧上界10.65%），排除端为0/150（单侧上界1.98%）。
这些数值是已选择阈值的透明风险描述，不恢复独立放行含义。

公开冻结配置为 `configs/cleaning-model-retraining-delivery.yaml`，决策ID
`f08dea56b16f1769006ae97d2704518d`。策略包
`8c87b85cd44803b9451c825bacaa3e90` 原样复用模型字节，模型SHA-256仍为
`972b678a3d8c49cd00570a72348b419ab3d20a92b99650011005cc1da38c6084`；策略
manifest SHA-256为
`5874882125b521271f99b990f7b7b5be38fcf4426aefb4fd1e1e1cb07d07667b`。
概率重分流 `06bdab80b623f51352582eb911d473f3` 不调用fit、predict或encoder，manifest
SHA-256为
`c95fb959184756e7aec8b1b3621a4b8580161f698dbfea02cc7e8e15a04b9b45`。

中间层任务 `ab6fae6ceab29bcc0254f84d49278fde` 排除已有300条审计标签后稳定产出
2,286行UTF-8 BOM四列表 `manual-review-tourism-relevance-annotation.csv`；人工
只填写 `tourism_label`。任务SHA-256为
`05fac84d995a555028ff213b93998f1e96473a0e1e21497c48dc826a10e04413`。
最终派生决定 `e79dc72751e0c7a1af75ee2f1dfbb1d7` 覆盖13,858条：6,835条
`keep`、4,737条 `exclude`、2,286条 `manual_review`。其中1,300条训练标签和
300条审计标签人工优先；模型实际自动保留6,111条、自动排除3,861条。决定
manifest SHA-256为
`b53d8f012dda5bbc19f1d1a6cd84fa728d500f304b99260b6ea1bf4dd1e07101`；源库写入、
源记录删除、fit、predict和encoder调用均为0。模型研发至此结束，中间层只需由
稳定任务表承接，不再扩大模型或阈值搜索。

## 生产推理与交付收口

训练阶段与当前13,858条派生人口的路由决定均已完成，生产入口不得重新训练，也
不得重建当前人口。`ROUTING_DELIVERABLE_READY` 指全体记录均已进入 `keep`、
`exclude` 或 `manual_review` 之一；其中2,286条中间层仍等待人工表中的二元或
`uncertain` 标签，不能写成已由模型自动确定。

未来记录只有 `scripts/cleaning_predict_tourism_relevance.py` 一个生产入口。它
读取严格六列CSV：`source_post_id`、`source_version`、`component_id`、`title`、
`body`、`source_status`，加载上述唯一融合包和0.31/0.96交付策略，执行程序内
规范化、完整连续分块与纯预测；不提供训练、校准、候选选择或当前派生库重跑
子命令。已完成的 `cleaning_retrain_routing_model.py` 只保留研究谱系复现能力，
不再暴露 `score-population` 或 `score-new-batch`。

Qwen向量缓存以规范正文SHA寻址，同时绑定完整分块算法、重训计划与Qwen权重
快照。同一帖子在正文未变时可跨批次、跨进程复用向量；正文或任一冻结身份变化
时进入新缓存键。既有缓存不完整、摘要不符或向量损坏时失败关闭，不允许静默
重算覆盖。每次运行封存checkpoint、缓存命中/新编码数量、完整预测CSV、人工
中间层四列表、私有映射和manifest，并保持 `fit_call_count=0`、源数据库写入与
删除为0。
