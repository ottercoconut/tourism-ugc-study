# 新全量清洗运行记录

## Material Passport

- Origin Skill: academic-research-suite / experiment-agent
- Origin Mode: run
- Origin Date: 2026-09-11
- Verification Status: UNVERIFIED（输入已核验，模型全量结果尚未验收）
- Version Label: research-cleaning-20260911

## 决定与范围

研究者授权使用新的全量研究快照进行一次清洗，并做好版本管理。沿用唯一冻结
融合模型与0.31/0.96阈值，不重训、不校准、不重新选择阈值。原始采集库只读，
旧keep表保留结构且已清空；本轮先交付候选结果，人工审查确认后才能发布和冻结
最终研究数据集。研究者没有授权把模型候选直接当作最终纳入，也没有要求管理端
在此次交接前进入实现阶段。

远程仓库仍为 `ottercoconut/tourism-ugc-study`；工作分支
`research-cleaning-20260911`。已合并主线 `68f54c3` 的编码规范更新，未修改该更新
的研究编码含义。原stash与旧模型、人工证据、旧源快照、旧keep备份均保留。

## 输入与冻结依赖

| 项目 | 身份或数量 |
|---|---|
| 原始采集库 | `TripPostCollect/data/trippostcollect.sqlite`，只读 |
| 研究输入 | `topic_relevant=1`，19,255条，156,137条关联图片元数据 |
| 快照SHA-256 | `6af11d8f3e390bb3e9b0081b0a827159a549f18512d0a30eb30c0861d296cb2d` |
| 快照manifest SHA-256 | `3ca1930d251861cf779fa6cf22846bc1c676e59e443b880a46b090e1083280ed` |
| 规范化配置SHA-256 | `6bd3fd6a94605e6dcd54908fa98d47c05561e7275835f493c459af5380ad5360` |
| 冻结融合模型SHA-256 | `972b678a3d8c49cd00570a72348b419ab3d20a92b99650011005cc1da38c6084` |
| 路由策略manifest SHA-256 | `5874882125b521271f99b990f7b7b5be38fcf4426aefb4fd1e1e1cb07d07667b` |
| 运行配置SHA-256 | `68e5dd39c95a3cbcc4079432db1ca1b785c321345e3f426c4bf530277be458ae` |

1,402条同一帖子、规范正文完全一致的人工证据可承接：训练来源1,133条、既有审计
来源269条。其余17,853条通过原六列预测入口；不复用旧keep名单或旧概率。新增
6,035条、有效输入变化520条、未变12,700条。人工证据继承不是本轮终审批准。

确定性检查全部可用；19,031个精确簇中157个含多条，共224条额外精确副本；
1,062对近重复仅为候选，作者/精确关系形成11,569个泄漏分量。不按这些关系自动
删除或传播标签。这里不是最终去重后研究样本量。

## 执行与恢复

类型为ETL加冻结模型推理，不是训练或新的性能验证。运行环境为CPython3.13.5、
macOS26.5.2 arm64、PyTorch2.13.0、Transformers5.15.1、scikit-learn1.9.0；本机MPS
可用，硬件Apple M5、统一内存16GiB。Qwen3-Embedding-4B按原2048 token连续完整分块、逐条编码、平均后L2规范化，
省略token数必须为0。数据不上传第三方推理服务。

每批250条，共72批；每批硬超时1,800秒，提前60秒告警。30秒更新一次执行事件，
进度看逐条checkpoint及向量缓存；失败停止，日志和证据保留。随机种子20260911
只用于审查材料顺序，不改变模型推理或去重规则。CPU确定性预处理与GPU推理应区分：
前者比较内容摘要；后者保留硬件/库版本及向量回执，不把耗时作为可重复性指标。

首次准备和执行代码为 `af06e3d36e8dba4e0f38f20dedccc2c828170755`，目录为
`results/research-cleaning-20260911/`。开始于2026-09-11 10:04:39 UTC；首批运行中
发现另有1条200,704字符模型输入将超过旧CSV读取器默认131,072字符字段上限，
故10:07:27 UTC主动停止本任务子进程，留下110条checkpoint与有效向量缓存。
这是为修复已确认输入兼容问题而主动停止，不是模型失效或主动修改标签。

修复代码为 `a6f3f9ba0985d6d4084f45a6881cfb4801b2d4b9`：CSV读取期间按文件字节数
放宽字段上限，结束后恢复原值，不截断、不修改模型。修复后的完整轮次目录为
`results/research-cleaning-20260911-r2/`，与首次尝试分别封存；旧缓存只经摘要校验后
复用。运行通过 `results/code-snapshots/a6f3f9b/` 的独立固定检出执行，后续文档编辑
不改变执行代码身份。完整命令、运行起止、PID和每批输出摘要保存在私有执行账本。

r2输入manifest SHA-256为
`f33b41fb3569541de13206b033b2c39eb91325aa7c271b2237cd58d9bd99d595`，
于2026-09-11 10:13:53 UTC启动。启动前已实际逐批读取全部72批17,853条，长字段
完整通过；r2与首次准备的正文、源版本、精确/近重复和泄漏分组内容摘要全部相同。

准备命令（项目根目录）为：

```sh
.venv/bin/python -u scripts/cleaning_prepare_research_round.py --output results/research-cleaning-20260911-r2
```

推理使用 `cleaning_run_research_round.py`，显式提供项目根、轮次根和该轮
`round-manifest.json` 的实际完整摘要；不能把首次尝试的manifest摘要用于r2。
装配使用 `cleaning_assemble_research_round.py`，需所有原生批次完成并核验后才可
生成 `cleaning-candidates.sqlite` 和 `candidate-manifest.json`。

## 验收门与已知问题

- 清洗相关123项测试通过，包含超长字段完整性、正文/分量错配拒绝、人工与模型隔离、
  概率及动作一致性、候选库完整性、盲四列表和缓存契约。
- 全仓测试为332通过、1失败、7条已有依赖弃用警告。唯一失败是主线已有的研究内容
  编码模板版本对齐：`data/annotations/README.md` 声明v3.16.0，而主线编码表已为
  v3.18.0。已核对`origin/main`同样如此；本轮旅游相关性清洗没有V0—V11语义依赖，
  不擅自改编码或虚报全仓测试通过。
- 最终验收必须做到19,255条一一覆盖、17,853条模型输入一一对应、旧人工证据匹配、
  所有批次/正文/概率/动作摘要有效、源快照不变、源库零写入、正式keep仍未自动发布。
- `CANDIDATES_READY_AWAITING_HUMAN_REVIEW`仅是后续候选交付状态；在完整产物实际
  生成并验收之前，不报告实际keep数、模型全量完成或最终研究集冻结。

方法与工程的共同边界见《[数据清洗科研方案](../methods/数据清洗科研方案.html)》、
《[数据清洗工程方案](../protocols/数据清洗工程方案.md)》。旧成员清空与备份见
《[源快照切换与旧清洗产物保留决定](2026-09-11-源快照切换与旧清洗产物保留决定.md)》。
