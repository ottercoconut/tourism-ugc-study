# 新全量清洗运行记录

## Material Passport

- Origin Skill: academic-research-suite / experiment-agent
- Origin Mode: run
- Origin Date: 2026-09-11
- Verification Status: VERIFIED（全量执行、输入/结果/向量完整性及远端释放已核验；不是新的独立准确率验证）
- Version Label: research-cleaning-20260911

## 决定与范围

**旧尝试终态：`CANCELLED_INFERENCE_ARTIFACTS_DESTROYED`。** 研究者决定换机器，
要求停止并销毁本机与服务器的本任务全部推理产物。已删除本机
`results/research-cleaning-20260911/`、`results/research-cleaning-20260911-r2/`和
`results/cleaning-model-inference-vector-cache/`，包括全部完整预测、部分断点、
批次输入副本、日志及向量缓存，不留备份。服务器本任务的独立临时环境、下载
缓存和代码副本亦已移除；服务器尚未开始实际模型推理。只保留本段无正文取消
记录，源库/源快照、冻结模型、此前人工证据与历史实验不在销毁范围。当前清洗
指针当时清空活动路径并禁止自动恢复。下文旧尝试过程不代表产物仍存在。

**AutoDL重新授权与准备阶段（历史）：`REMOTE_RESTART_PREPARING`。** 研究者提供AutoDL实例并授权保持
当前密码SSH登录、不修改防火墙/认证，在50GB数据盘上无人值守从头推理。实例
实测Ubuntu22.04、RTX4090 24GB、内存120GB、CPU16核；实例为受限容器，不具备
`CAP_NET_ADMIN`，未启用UFW。本任务不开放新服务，不改变任何安全或计费设置。

新轮次为`research-cleaning-20260911-autodl`，每批500条，共36批。只有当前一批
输入、公共编码器单份权重和冻结依赖上传；研究源库不上传。服务器单批完成后，
原生预测包、断点和日志回传本机，逐文件SHA、本轮输入回执及模型/代码身份全部
验证并持久化，才释放远端该批数据。转移/校验/释放失败不启动下一批。保留同一
远端PID/账本观察断连状态，不自动重试模型崩溃，不把部分结果发布为候选或最终keep。
公共软件直接下载超时时使用平台学术加速或本机转传；不更换冻结版本。

迁移实现定向测试26项通过；全仓实测351通过、1个原有编码表版本对齐失败、
7条已有依赖弃用警告。失败仍是内容标注README的v3.16.0与编码表v3.18.0不一致，
与旅游相关性推理无语义依赖；不为本次运行改动研究内容编码模板。

12:38 UTC，四条长度边界样本在CUDA完成并成功回传、验收、释放远端数据；
fit/源写入/训练重叠均为0。这仅验证执行链路，不是模型准确率检验。实测后发现
原生概率只引用向量回执摘要，因此在全量启动前增加向量与回执一同归档，并验证
零省略、float32、2560维、L2及数值哈希，防止清理缓存后形成无法追溯的摘要引用。
原输入准备未执行全量，保留为准备记录；实际全量改用`research-cleaning-20260911-autodl-r2`，
重新封存代码身份，不混用烟雾测试概率。服务器登录、防火墙、模型及研究算法未改。

### AutoDL全量实际启动

2026-09-11 12:49:14 UTC启动`research-cleaning-20260911-autodl-r2`，状态为
`FULL_INFERENCE_RUNNING`。固定执行/装配代码`3d1ef87f03891a79c155c81f0110542372ea697d`；
配置SHA-256 `f738bd4ff22547934d50c443f52b917975e9f7877a2b02cdfab526985c3eae0b`，
输入manifest SHA-256 `9ed72f27b9671c40014383bba28987eaae871f1a1726e52142c26f8a650277cd`。
记录、源版本和分组摘要与此前准备一致，36份CSV与已完成原生读入检查的17,853条
逐字节一致。全仓最新实测352通过、1个上述既有失败；迁移相关27项通过。

服务器CPython3.13.5、PyTorch2.13.0/CUDA13.0、Transformers5.15.1、
SentenceTransformers6.0.0、NumPy2.5.1、scikit-learn1.9.0，bfloat16矩阵运算和
`pip check`通过；Qwen全部文件与冻结快照/权重摘要一致。环境及公开权重验证回执
保存在私有运行目录。安装缓存已清理，运行时数据盘约13GB占用、38GB可用。

首批500条已于12:51 UTC回传、逐文件和逐条向量引用复核，通过后远端释放。
原生manifest SHA-256 `b4250c6682417fa67f85adda194f10e851db86b8d16f6a56335a53dca25e3e2f`，
传输manifest SHA-256 `91197e299d1298241deb3d0fe93a6683f41a36adfa029a682cc9d0d0546e1305`；
500条引用495份唯一正文向量，fit/源写入/训练重叠均为0。此计数仅为启动检查点，
最新进度以`execution-state.json`为准。已启动单次后处理，等待全部36批及回传验收
成功后才装配`data/processed/research-cleaning-20260911-autodl-r2/`并更新当前指针。
正式keep表仍为0，不自动发布最终纳入或宣称最终研究集冻结。

### AutoDL全量完成与最终验收

**当前终态：`CANDIDATES_READY_AWAITING_HUMAN_REVIEW`。** 2026-09-11 14:13:49 UTC，
36批、17,853条模型输入全部完成、回传验收并释放远端批次数据；14:13:52 UTC，
单次后处理合并1,402条同规范正文人工证据，生成覆盖19,255条的候选库，并更新
`data/processed/current-cleaning.json`。本轮未回填片段、未做内容标注或人工终审。

| 候选动作 | 帖子数 |
|---|---:|
| keep | 10,914 |
| exclude | 5,305 |
| manual_review | 3,036 |
| 合计 | 19,255 |

候选库为`data/processed/research-cleaning-20260911-autodl-r2/cleaning-candidates.sqlite`，
表为`cleaning_candidates`，一条post一个记录。决定来源为冻结模型17,853条、
训练人工证据1,133条、审计人工证据269条；人工证据行的模型概率为NULL。全部
19,255条`human_final_review_status=pending`，不是最终研究纳入。

| 交付证据 | SHA-256 |
|---|---|
| 候选数据库 | `ea280e14692ba0c2c257cf1b5062e54158feecc96f70449f3e11d9f268bd2595` |
| candidate-manifest.json | `4dc7eb9d7b2561c41d9ef1c362129b4832b03e4eae46e23855f04fd58ee6be23` |
| execution-state.json | `c90e848e68fc19936a315f8c9795c609156cc184d898662e589d483d1fd43aa9` |
| final-audit.json | `d9c98dfc91a16362bef3751b34f13af456f1bf11f67faddf3d0559f26cafa525` |

最终只读复核逐一验证36份传输清单、原生预测包、17,696份批内缓存向量及17,853条
概率引用：float32、2560维、有限值、L2、数值与文件摘要、正文摘要和零省略契约
全部有效。157条批内缓存命中不冒充新的编码调用。输入/候选身份、源版本、正文、
规范文本、泄漏分量、人工证据来源、冻结0.31/0.96动作及每条证据摘要逐一吻合。
fit、源数据库写入/删除、训练成员重叠、模型断点重试均为0；这不是准确率检验。

keep-review.csv为10,914条，manual-review-review.csv为3,036条；四列结构和私有
映射完整，`tourism_label`全空。1,062对近重复候选保留；6个精确簇的候选动作
冲突已显式标记，未自动删除或传播标签。keep涉及10,894个精确簇，不作为最终
去重后的独立研究样本量。最长原六列CSV正文200,704字符，最长规范模型文本
99,964字符；两者口径不同，均未因读取上限而截断。

第24批于13:43:08 UTC完成后，本机在13:44:12 UTC因SSH master broken pipe失去
观察连接，调度和首次后处理按保护规则退出，未生成部分候选。重连核对原PID
33354及启动标识`3265142156`，确认COMPLETED、500条结果及完整向量仍在远端，
随后重新验收本机前23批，并于13:53:15 UTC接收第24批后继续未启动批次；没有
重新推理前24批或修改冻结代码/模型。恢复证据为私有运行目录中的
`network-recovery-20260911T134412Z.json`，SHA-256为
`aeafd1f832aba4aeb6b38d9db6e2e826e0b31e5f0e608ce0304129846d225480`。

最终服务器36个RELEASED回执均与本机传输摘要一致；本任务推理进程、批次输入、
批次输出和批次向量缓存均为0，GPU无计算进程，数据盘仍约38GB可用。公共编码器、
冻结依赖与运行环境保留，实例未关机、登录模式和防火墙未改。原源快照摘要仍为
`6af11d8f3e390bb3e9b0081b0a827159a549f18512d0a30eb30c0861d296cb2d`；
正式keep库仍0行，数据库摘要仍为
`414f073156ad1faf1ac5374a8ba86427ee515e1e4de2db07aedac69e1c78fd45`，配对manifest
摘要仍为`64748f33956e039f523422669735034df9e085d350e8cf3fec5950d9fe1e6d92`。

已向管理端任务“等待研究页面交接”（`01a06b78-6c5b-7341-9492-beb2f41f2fe9`）发送
数据库路径、字段、计数、完整摘要及人工边界，开头明确要求“不要进入实现阶段，
交接完之后先和用户讨论怎么设计”。没有授权改写候选快照、发布正式keep或直接
进入管理端实现。

全量完成后，轮次准备、执行、候选装配及远端回传四组相关回归测试45项全部
通过，`git diff --check`通过。此前全仓实测352通过、1个主线既有编码表版本
对齐失败的记录仍保留，不将局部回归通过写成全仓零失败。

研究者授权使用新的全量研究快照进行一次清洗，并做好版本管理。沿用唯一冻结
融合模型与0.31/0.96阈值，不重训、不校准、不重新选择阈值。原始采集库只读，
旧keep表保留结构且已清空；本轮先交付候选结果，人工审查确认后才能发布和冻结
最终研究数据集。研究者没有授权把模型候选直接当作最终纳入，也没有要求管理端
在此次交接前进入实现阶段。

远程仓库仍为 `ottercoconut/tourism-ugc-study`；工作分支
`research-cleaning-20260911`。已合并主线 `68f54c3` 的编码规范更新，未修改该更新
的研究编码含义。原stash与旧模型、人工证据、旧源快照、旧keep备份均保留。

## 输入与冻结依赖

| 项目 | 身份或数量 |
|---|---|
| 原始采集库 | `TripPostCollect/data/trippostcollect.sqlite`，只读 |
| 研究输入 | `topic_relevant=1`，19,255条，156,137条关联图片元数据 |
| 快照SHA-256 | `6af11d8f3e390bb3e9b0081b0a827159a549f18512d0a30eb30c0861d296cb2d` |
| 快照manifest SHA-256 | `3ca1930d251861cf779fa6cf22846bc1c676e59e443b880a46b090e1083280ed` |
| 规范化配置SHA-256 | `6bd3fd6a94605e6dcd54908fa98d47c05561e7275835f493c459af5380ad5360` |
| 冻结融合模型SHA-256 | `972b678a3d8c49cd00570a72348b419ab3d20a92b99650011005cc1da38c6084` |
| 路由策略manifest SHA-256 | `5874882125b521271f99b990f7b7b5be38fcf4426aefb4fd1e1e1cb07d07667b` |
| 当前AutoDL运行配置SHA-256 | `f738bd4ff22547934d50c443f52b917975e9f7877a2b02cdfab526985c3eae0b` |

1,402条同一帖子、规范正文完全一致的人工证据可承接：训练来源1,133条、既有审计
来源269条。其余17,853条通过原六列预测入口；不复用旧keep名单或旧概率。新增
6,035条、有效输入变化520条、未变12,700条。人工证据继承不是本轮终审批准。

确定性检查全部可用；19,031个精确簇中157个含多条，共224条额外精确副本；
1,062对近重复仅为候选，作者/精确关系形成11,569个泄漏分量。不按这些关系自动
删除或传播标签。这里不是最终去重后研究样本量。

## 执行与恢复

### 用户要求转移到服务器后从头运行

2026-09-11 11:23:53 UTC，按研究者要求停止本地r2与自动后处理。已完整封存7批、
1,750条，另有第8批173条checkpoint；退出码130表示用户主动中断，不是模型失败。
这些产物当时保留、随后按用户要求销毁，服务器不承接其中的概率或向量。研究者随后指定`ssh runpod`，
授权配置既有服务器并从头运行，未创建新的付费Pod或改动其他项目。

实测目标为Linux x86_64、RTX 4090（24GB），GPU起初空闲。独立运行配置
`cleaning-inference-cuda.yaml`仅指定CUDA、同帖完整视图batch=8及原bfloat16/float32
精度；配置摘要、实际软件版本进入新缓存命名空间和推理manifest。原训练计划、
编码器权重/指令/2048-token完整分块/平均后L2聚合、模型与0.31/0.96阈值不变。
跨MPS/CUDA不保证逐位相同，正式服务器结果须独立验收，不能混用本地完成包。

当时服务器计划使用相同研究输入与人工证据规则，调度改为
每批500条、共36批，30分钟批超时仍保留；新轮次与旧目录分开。服务器环境使用
独立CPython3.13.5，不修改已有项目。安装依赖时持久化盘返回配额不足；共享池的
`df`空闲不代表用户卷配额，故可重建环境/公开权重须与持久化研究产物分开安排。
当时停留在`REMOTE_RESTART_PREPARING`，该Runpod迁移准备后来被取消并销毁；
本段不代表当前AutoDL状态。

### 本地历史尝试

类型为ETL加冻结模型推理，不是训练或新的性能验证。运行环境为CPython3.13.5、
macOS26.5.2 arm64、PyTorch2.13.0、Transformers5.15.1、scikit-learn1.9.0；本机MPS
可用，硬件Apple M5、统一内存16GiB。Qwen3-Embedding-4B按原2048 token连续完整分块、逐条编码、平均后L2规范化，
省略token数必须为0。数据不上传第三方推理服务。

每批250条，共72批；每批硬超时1,800秒，提前60秒告警。30秒更新一次执行事件，
进度看逐条checkpoint及向量缓存；失败停止，日志和证据保留。随机种子20260911
只用于审查材料顺序，不改变模型推理或去重规则。CPU确定性预处理与GPU推理应区分：
前者比较内容摘要；后者保留硬件/库版本及向量回执，不把耗时作为可重复性指标。

首次准备和执行代码为 `af06e3d36e8dba4e0f38f20dedccc2c828170755`，目录为
`results/research-cleaning-20260911/`。开始于2026-09-11 10:04:39 UTC；首批运行中
发现另有1条200,704字符原始正文字段将超过旧CSV读取器默认131,072字符字段上限，
故10:07:27 UTC主动停止本任务子进程，留下110条checkpoint与有效向量缓存。
这是为修复已确认输入兼容问题而主动停止，不是模型失效或主动修改标签。

修复代码为 `a6f3f9ba0985d6d4084f45a6881cfb4801b2d4b9`：CSV读取期间按文件字节数
放宽字段上限，结束后恢复原值，不截断、不修改模型。修复后的完整轮次目录为
`results/research-cleaning-20260911-r2/`，与首次尝试分别封存；旧缓存只经摘要校验后
复用。运行通过 `results/code-snapshots/a6f3f9b/` 的独立固定检出执行，后续文档编辑
不改变执行代码身份。完整命令、运行起止、PID和每批输出摘要保存在私有执行账本。

r2输入manifest SHA-256为
`f33b41fb3569541de13206b033b2c39eb91325aa7c271b2237cd58d9bd99d595`，
于2026-09-11 10:13:53 UTC启动。启动前已实际逐批读取全部72批17,853条，长字段
完整通过；r2与首次准备的正文、源版本、精确/近重复和泄漏分组内容摘要全部相同。

准备命令（项目根目录）为：

```sh
.venv/bin/python -u scripts/cleaning_prepare_research_round.py --output results/research-cleaning-20260911-r2
```

推理使用 `cleaning_run_research_round.py`，显式提供项目根、轮次根和该轮
`round-manifest.json` 的实际完整摘要；不能把首次尝试的manifest摘要用于r2。
装配使用 `cleaning_assemble_research_round.py`，需所有原生批次完成并核验后才可
生成 `cleaning-candidates.sqlite` 和 `candidate-manifest.json`。

`cleaning_complete_research_round.py`作为同次流水线的单次后处理，等待当前推理
释放执行锁，再验证 `BATCHES_SCORED`、装配候选并原子更新当前清洗指针；不重试
模型，不发布部分结果，也不修改最终keep表。候选输出指定为
`data/processed/research-cleaning-20260911/`，在实际完成前该目录不存在。后处理自身
的提交、状态、输出摘要单独写入 `completion-state.json`，与推理版本分开记录。

## 验收门与已知问题

- 清洗相关130项测试通过，包含超长字段完整性、正文/分量错配拒绝、人工与模型隔离、
  概率及动作一致性、候选库完整性、盲四列表、缓存契约、全轮装配与失败不发布。
- 全仓测试为339通过、1失败、7条已有依赖弃用警告。唯一失败是主线已有的研究内容
  编码模板版本对齐：`data/annotations/README.md` 声明v3.16.0，而主线编码表已为
  v3.18.0。已核对`origin/main`同样如此；本轮旅游相关性清洗没有V0—V11语义依赖，
  不擅自改编码或虚报全仓测试通过。
- 最终验收必须做到19,255条一一覆盖、17,853条模型输入一一对应、旧人工证据匹配、
  所有批次/正文/概率/动作摘要有效、源快照不变、源库零写入、正式keep仍未自动发布。
- `CANDIDATES_READY_AWAITING_HUMAN_REVIEW`仅是后续候选交付状态；在完整产物实际
  生成并验收之前，不报告实际keep数、模型全量完成或最终研究集冻结。

方法与工程的共同边界见《[数据清洗科研方案](../methods/数据清洗科研方案.html)》、
《[数据清洗工程方案](../protocols/数据清洗工程方案.md)》。旧成员清空与备份见
《[源快照切换与旧清洗产物保留决定](2026-09-11-源快照切换与旧清洗产物保留决定.md)》。
