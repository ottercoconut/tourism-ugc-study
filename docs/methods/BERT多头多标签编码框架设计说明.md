# 青岛旅游 UGC 的 BERT 多任务、多头、混合输出编码框架设计说明

> **本轮待同步范围**：现行V0为证据优先、粉丝最后辅助判定；五平台记录规模，仅小红书／知乎判角色；共存者满足条件归KOL，HYBRID只保留历史值。本文旧角色正文仍为NEEDS_UPDATE，不得直接发包。
> **文档状态**：`NEEDS_UPDATE`（仅V0角色解释；V3—V6映射不受本轮影响）
> **当前上位标准**：[`编码表.md`](../data-dictionary/编码表.md) v3.18.0；下述正文对齐基线为v3.16.0
> **2026年9月8日范围提示**：V0角色命名、五档粉丝规模及未决校准门以v3.18.0为准，本文尚未完整承接该部分；本轮未改变文本BERT的字段、输出头或人工金标责任，不据此启动角色模型工程迁移。
> **内部文档版本**：`v3.16.0-alignment.1`
> **执行成熟度**：`COMMON_CALIBRATION_PACKAGE_GENERATED / PILOT_ONLY / NOT_FROZEN`；本文件只定义候选模型映射，正式训练须等待相关字段完成独立信度冻结
> **权威边界**：编码表定义构念、人工字段和值域；固定Excel编码簿内部模板2.6承载人工金标；本文不得删除、合并或自动派生人工标签
> **任务范围**：文本BERT仅覆盖固定`seg_id`上的V3、V4、V5和V6。V0为隔离的作者级角色任务，帖子级字段由独立任务处理，V7—V11由视觉或多模态模型处理。主动学习、数据增强、选择性输出和概率校准保留为原研究设计的候选实验，本轮编码方法改进不删除或强制启动它们。若以后启动主动学习，只能在最终模型锁定前另立轮次，模型只选样、不向编码员建议标签；进入非金标生产编码后不再人工回流。

## 一、问题定义

文本模型需要复现编码员对固定评价原子片段的逐字段判断，而不是直接预测论文中的帖子级指数。当前人工编码面共有79个文本标签列和2个人工强度量表字段；每个主观字段旁可保留可选疑问标记列，但该过程标记不作为监督目标、输入特征或损失权重。

```text
固定raw_text与seg_id
        ↓
共享文本编码器
        ↓
V3 / V4 / V5 / V6技术头
        ↓
逐字段概率、类别或强度输出
        ↓
一致性审计与后续帖子级统计
```

评价对象或情感方向发生切换时，切分程序必须先产生新的`seg_id`。模型不得在一个片段内跨对象配对V5情感与V6属性，也不得自行改变切分边界。

## 二、设计原则

1. **逐字段输出**：人工编码员填写的每个父类、子类、排除项、适用性和量表字段均保留模型输出。
2. **值域决定技术头**：可共现二元字段用Sigmoid；冻结为互斥的类别轴可用Softmax；0/1/2强度优先使用序数头或经比较验证的三分类头。
3. **父子关系不覆盖输出**：父子、适用性和“其他/无”关系只用于错误审计或候选层级正则，不得补写人工值或覆盖模型概率。
4. **结构性NA掩码**：`NA`不作为负例；`UNK`和未关闭的`UNRESOLVED`也不进入正式训练损失。
5. **文本证据可追溯**：`evidence_spans_json`可用于解释、证据抽取或错误分析，但不默认把attention权重解释为证据。
6. **聚合后置**：标签次数、片段比例和是否出现是基础帖子级汇总；密度、广度、轮廓、SRM、SAM、AAM及其他指标仍保留原设计状态。它们均在逐字段预测后计算，不反向成为训练标签。

## 三、监督字段与技术头

### 3.1 V3内容策略：27个二元输出

| 组 | 输出字段 | 建议头 |
|----|----------|--------|
| 5个父类 | `cs_inf`、`cs_edu`、`cs_emo`、`cs_int`、`cs_oth` | 5个Sigmoid |
| 信息提供子类 | `cs_inf_itn`、`cs_inf_par`、`cs_inf_pro`、`cs_inf_oth` | 4个Sigmoid |
| 知识阐释子类 | `cs_edu_bkg`、`cs_edu_cau`、`cs_edu_mea`、`cs_edu_oth` | 4个Sigmoid |
| 情感渲染子类 | `cs_emo_sen`、`cs_emo_atm`、`cs_emo_aff`、`cs_emo_oth` | 4个Sigmoid |
| 互动激发子项 | `is_que`、`is_dir`、`is_soc`、`is_oth`、`is_non` | 5个Sigmoid |
| 其他策略子类 | `cs_oth_gre`、`cs_oth_str`、`cs_oth_met`、`cs_oth_sym`、`cs_oth_res` | 5个Sigmoid |

旧编号V5已并入V3-CS-INT子项层。`cs_int`和`is_non`仍有独立人工金标，不能由互动子项的OR或零向量代替。

### 3.2 V4目的地资源：16个二元输出

`rs_n`、`rs_r`两个Layer 1父类，10个Layer 2亚类，以及`evt_spt`、`evt_per`、`evt_fes`、`evt_oth`四个Layer 3事件子类都由编码员判断，因此使用16个Sigmoid输出。Layer 1—Layer 2及`rs_r_evt`—事件子类关系只用于一致性审计；模型必须同时报告各层概率。`rs_r_act`不是人工金标，不设输出头，只能在二值预测之后由`rs_r_rec OR rs_r_evt`汇总。

### 3.3 V5语言功能与情感：混合输出

| 组 | 人工列/候选输出 | 技术形式 |
|----|-----------------|----------|
| 语言功能 | `at_has_info`、`at_has_eval`、`at_has_sug`、`at_non` | 4个Sigmoid |
| AT-EVL亚类 | ADM、SAT、EXC、DIS、FRU、NOS、NA七个原子列 | 保留7个输出；单选Softmax或多选Sigmoid待专门规则冻结 |
| 情感可判定 | 0、1两个原子列 | 二分类头；输出保留两类概率 |
| 情感方向 | POS、NEG、MIX、NEU、NA五个原子列 | 条件Softmax；冻结前按编码表掩码 |
| 情感强度 | `sentiment_pos_val`、`sentiment_neg_val` | 两个0/1/2序数头；结构性NA掩码 |

V5不再包含目的地属性。任何净情感值只能在分析阶段由双向强度计算，不能成为覆盖人工量表的模型主输出。

### 3.4 V6目的地属性：18个条件输出

`asp_applicable`、4个父类、12个方向中立子类和`asp_oth`均由编码员判断。适用性可使用二元头；其余17个字段使用条件Sigmoid。当人工`asp_applicable=0`时，其余V6字段为结构性`NA`并从损失中屏蔽。

模型不能用V5情感方向推导V6属性，也不能仅预测12个子类后自动补写父类。V5与V6的关联只在同一`seg_id`内用于下游AAM或一致性审计。

## 四、基线架构

```text
输入片段
  ↓
预训练中文文本编码器
  ↓
共享片段表示 h
  ├── V3：27维Sigmoid
  ├── V4：11维Sigmoid
  ├── V5功能：4维Sigmoid
  ├── V5亚类：7维条件输出（形式待冻结）
  ├── V5可判定：2类输出
  ├── V5方向：5类条件输出
  ├── V5强度：2个序数输出
  ├── V6适用性：1个二元输出
  └── V6属性：17维条件Sigmoid
```

首个可复现实验使用共享`[CLS]`表示和简单线性头。标签级注意力、Classifier Chains、图神经网络或长文本层级模型都保持`DEFERRED`，只有在独立逐字段基线暴露稳定错误模式且样本量支持时才进入消融实验。

## 五、损失与掩码

多标签字段使用带掩码的二元交叉熵：

\[
L_{bin}=\sum_k m_k\,\operatorname{BCE}(y_k,p_k)
\]

其中`m_k=0`表示该字段为`NA`、允许的`UNK`或无有效人工金标。互斥类别使用交叉熵，强度字段使用序数损失或三分类交叉熵，并通过开发集比较决定。

总损失可写为：

\[
L=\lambda_1L_{V3}+\lambda_2L_{V4}+\lambda_3L_{V5}+\lambda_4L_{V6}
\]

初始实验采用等权或按有效样本数归一化。人工疑问标记不得用作样本权重。类别不平衡优先通过训练集加权、Focal Loss或定向补充训练样本处理，锁定测试集分布不得因调参改变。

## 六、层级一致性与错误审计

以下关系只产生审计标记，不改写输出：

- V3正向子类为1而父类为0，或父类为1但无正向子类；
- `is_non=1`却存在正向互动子项，或`cs_int=1`且`is_non=1`；
- V4亚类为1但对应Layer 1父类为0；
- `at_non=1`却同时命中信息、评价或建议；
- `asp_applicable=0`但V6属性不是`NA`，或适用时没有任何子类/其他项；
- V6子类为1但对应父类为0。

候选层级正则必须作为消融项报告，并验证是否改善逐标签表现，而不是只改善逻辑一致率。任何规则后处理都须同时报告原始模型结果和后处理结果。

## 七、训练数据与证据

- 按`author_id`和重复簇分组切分训练、开发和测试，防止作者或近重复泄漏。
- 共同校准材料可以改规则，不能进入正式信度估计；规则冻结后另取新样本独立盲试标。
- 锁定测试集全部双人独立编码并裁决；训练池可由主编码员标注并对随机、带疑问标记、稀有和冲突样本复核。
- `UNRESOLVED`必须在盲试标前关闭并带疑问标记；疑问标记只作锁定后人工复核线索，不自动删除样本，也不进入模型。
- `evidence_spans_json`可用于证据重合率、错误定位或可选span任务；主分类基线不依赖人工证据才能推理。

## 八、评估

每个二元标签报告Precision、Recall、F1、PR-AUC、阳性数和不确定性区间，再报告Macro/Micro-F1。互斥或条件类别报告逐类指标、Macro-F1和混淆矩阵；强度字段报告有序加权指标及逐级误差。

同时保留以下效度诊断：

- 概率校准（Brier score或ECE）；
- 分平台、创作者层级和文本长度子群表现；
- 结构性NA比例、覆盖率与选择性复核成本；
- 父子冲突率和条件状态冲突率；
- 使用人工金标与模型预测得到的关键帖子级/作者级估计差值。

Exact Match只能作为补充指标，不能替代逐标签结果。任何正式自动化必须在锁定金标上达到预注册发布门；低频或低信度字段不得因总体Micro-F1较高而被掩盖。

## 九、视觉任务边界

V7—V11不进入文本BERT。V11的10个条件原子字段在视觉人口、对象触发规则、最低可见范围、NA/0/UNK边界和独立信度冻结前不得训练或发布。获准后也须逐字段输出，不得用单一视觉情感分数、图像美学分数或文本情感替代。

V0也不进入本模型。其观察单位是作者证据窗口而非固定文本片段，且须执行T0作者证据与T1目标内容隔离。V0的EA、CE、SC、证据充分性和最终角色均为直接人工金标；角色规则v0.2、50名新作者独立盲试标、作者级信度、组别支持门以及独立作者级模型锁定测试通过前，不得用V1—V6文本模型或目标帖表示预测`creator_role`。通过后人工退出非金标生产编码，程序不得回写人工金标。

## 十、当前实现结论

1. 当前首选文本基线是共享编码器 + 逐字段混合输出头。
2. V3、V4和V6父类与子类都保留输出；所有“无/其他”人工字段也保留；V4分析汇总`rs_r_act`不设输出头。
3. V5情感和V6属性是两个任务族，通过固定评价原子片段配对。
4. `at_eval_subtype`最终采用单选还是多选，须在共同校准和独立信度后冻结。
5. 帖子级标签次数、片段比例和是否出现是基础后置计算；原有轮廓、共现指标、复杂标签依赖、继续预训练和多模态融合按各自既定状态保留，不因本轮编码方法改进而删除。

## 文档迭代记录

| 内部版本 | 日期 | 变更 |
|----------|------|------|
| v3.4.2 | 2026-08-11及以前 | 模型设计正文历史基线 |
| v3.6.0-alignment.1 | 2026-08-14 | 同步共同校准记录契约、统一文本offset和稳定文件名 |
| v3.6.1-alignment.1 | 2026-08-15 | 同步编码员单表与规范标签输出边界 |
| v3.9.0-alignment.1 | 2026-08-21 | 重整为V1/V2/V3/V4逐字段任务结构，加入评价原子切分与V16训练冻结门 |
| v3.9.0-alignment.2 | 2026-08-21 | 同步固定Excel内部模板2.3的上级分类字段单列设计；文本任务结构与冻结门不变 |
| v3.10.0-alignment.1 | 2026-08-21 | V2改为16个二元输出，新增休闲游憩、节事文体事件及四个事件子类；`rs_r_act`转为无训练头的分析汇总；模板升至2.4 |
| v3.11.0-alignment.1 | 2026-08-22 | 文本任务连续改标为V3—V6、视觉任务改标为V7—V11；字段名、输出头、掩码和损失责任不变；模板升至2.5 |
| v3.13.0-alignment.1 | 2026-08-26 | 同步V0作者任务与V3—V6文本BERT的隔离边界；共同校准包仍为PILOT_ONLY，不构成正式训练金标 |
| v3.14.0-alignment.1 | 2026-08-26 | 同步V0完全人工金标、人工退出节点和非金标程序扩展边界；文本BERT任务结构不变 |
| v3.14.0-alignment.2 | 2026-08-31 | 标签输出结构不变；主评价收缩为逐标签Precision/Recall/F1与macro/micro F1，简单帖子聚合启用，主动学习、复杂校准和自定义指标改为可选 |
| v3.14.0-alignment.3 | 2026-08-31 | 撤回研究设计与模型评价简化；保留原模型效度、主动学习和聚合候选设计，本轮只改进底层人工编码方法 |
| v3.14.0-alignment.4 | 2026-09-01 | 明确主动学习只可在最终模型锁定前另立轮次，模型不向编码员建议标签；非金标生产阶段不再人工回流 |
| v3.16.0-alignment.1 | 2026-09-05 | 将人工逐字段五级置信度替换为可选疑问标记；明确其不进入模型输入、监督目标、样本权重或默认筛除规则。文本标签结构不变 |

*固定文件名不变；版本只在文档内部记录。V0角色解释为NEEDS_UPDATE，V3—V6映射不受本轮影响；发生冲突时，以编码表v3.18.0为准。*
