# 数据清洗工程方案

> **状态**：`MODEL_AND_ROUTING_FROZEN / POPULATION_SCORING_PENDING / AUTOMATION_NOT_AUTHORIZED`
>
> **稳定契约名**：`final-nonduplicate-model-reference`
>
> **标签手册**：`text-cleaning-v1.5`
>
> **当前派生 schema**：`34`

本文件定义正式工程框架。最终700条、finalized leakage build、baseline、sparse challenger、Qwen 三层、Wave A与Wave B均已不可变封存。研究者确认的 Qwen `T_keep=0.14 / T_exclude=0.86` 已在独立 Wave B 上完成描述性评价；相近人工量 sparse comparator 为 `0.20/0.80`。锁定测试已按预冻结规则单次执行并判为 `FAILED_MANUAL_ONLY`；正式全量/增量自动推理、部署审计和发布均未获授权。人类可读清洗文档不显示协议版本号；技术 artifact 继续使用内部 ID、哈希和谱系。

Issue #49 的新周期以前瞻性协议取代新运行的零错误门，但不改写上述历史结论。
`configs/cleaning-model-retraining.yaml` 固定1,300条证据、三个候选、完整token
分块、5折分组OOF、2%/5%自动尾部点风险门和150+150新盲审。新模型、阈值和
盲审尚未完成，故当前仍不允许正式自动路由。

## 0. 新模型周期的稳定工程契约

1. `freeze-snapshot` 只读联结 final 700、Wave A 240、Wave B 360与finalized
   leakage build。私有训练记录按内容寻址封存；公开摘要不含正文、身份或路径。
2. 旧148条测试成员标为 `historical_test_consumed`。它们可进入新训练，但所有
   新运行均不得提供“重新打开历史测试”的接口。
3. 完整文本编码把内容token切成连续、不重叠、无间隙的块；块长度与instruction
   合计不超过2048，省略token数必须为0。块向量算术平均后逐记录L2规范化。
4. 固定候选只有Qwen聚合向量＋LinearSVC、字符TF-IDF＋LinearSVC、交叉拟合
   logit逻辑回归融合。平台、样本框、旧概率、抽样层和Wave权重无法进入fit。
5. 三候选都保存leakage-group OOF margin和交叉拟合Sigmoid概率。Wave B权重只
   在OOF路由评价时读取；每个自动尾部至少30条原始标签。
6. 风险合格组合中按人工率、UGC误删、无关误留、log loss、复杂度和候选ID选出
   唯一策略。固定候选完成后即停止模型研发，改善不足也不扩展搜索。
7. 正式推理只有`predict`路径，`fit_call_count=0`，支持checkpoint、严格复用和
   批次幂等。训练成员跳过；审计成员以人工标签覆盖；源库始终只读。
8. 新双尾审计每尾150条。排除端至多3个`related/uncertain`，保留端至多7个
   `unrelated/uncertain`；不允许补抽或阈值稀释。单尾失败只降级该动作。

稳定薄CLI为 `scripts/cleaning_retrain_routing_model.py`，子命令依次是
`freeze-snapshot`、`encode-qwen`、`train-candidates`、`freeze-routing`、
`score-population`、`prepare-audit`、`assess-audit` 和 `build-decisions`。
每个状态变化都要求显式执行开关和上游外部manifest哈希。训练入口没有平台或
`sample_weight`参数；推理、审计和决定入口没有fit参数。人口评分逐记录保存
checkpoint及滚动摘要，恢复时重验成员顺序、正文哈希、概率动作和策略身份。

`prepare-audit` 一次冻结两个尾部各150条；任务表只含固定四列，不显示动作、
概率、模型或入选原因。`assess-audit` 只接受恰好300条同批次完成表，3/7条为
通过边界、4/8条为失败边界。区间只完整披露。`build-decisions` 对训练1,300条和
审计300条使用人工覆盖，对未通过尾部改为 `manual_review`，只把通过尾部写成
派生自动动作；它明确记录源库写入数和删除数均为0。

正式训练快照 `6f1075ec9b70ca1485208762cb7a6453` 已在提交 `e89a467`
身份下封存，manifest SHA-256 为
`bd8142bfe5340895c23c837063bcad6c21a7fa0506ccd914f74f3370562048fa`。
实际联结结果为1,300条、585 related、715 unrelated、1,169个分量、148条
`historical_test_consumed`；抽样权重与平台进入fit的计数均为0。

完整编码运行 `0d1a5bb7eb05fca01f943ae549a11bbe` 绑定算法
`qwen-complete-contiguous-chunks-preserve-content-tokens-v2` 和提交 `9828bc3`。
1,300条共生成1,600个视图，138条为多视图，最长记录36视图、原输入70,093
tokens，57个视图因解码边界收窄；所有余下token均顺延到下一视图，总省略为0。
编码manifest SHA-256为
`c02ad53e8ec2baf8d73148bf7f70313dfad0d3e6eff464a74c3709bd79f7b81d`。

固定三候选运行 `b89e34ad89bdc954de7e7ed540a710c2` 使用5折分组OOF。
0.5诊断下，Qwen、sparse、融合的Accuracy依次为91.54%、86.85%、91.62%，
log loss为0.2357、0.3167、0.2305；训练manifest SHA-256为
`875b625a13e1609dce2cecc53370a1312b53103988fb7ee27893320866f2960f`。
这些是训练内OOF诊断，不是旧测试的新结果。

策略 `c4b708a22568e28f433f710973213672` 选择融合候选
`721a2601f8ba8622257c1067ae85385b` 与 `T_keep=0.44 / T_exclude=0.96`。
Wave B设计加权人工率16.46%，相对历史20.36%降低3.90个百分点；自动排除端
related点风险0%，自动保留端unrelated点风险4.81%。原始支持为156/115/89，
原始不利事件为保留端19、排除端0；原始Clopper–Pearson和component bootstrap
只披露、不作门。策略manifest SHA-256为
`3ee6d3c90912e7fcc9ff3229cfeac70cd6e28ec62cc77e5dcc7892850377323c`。
固定候选比较已经结束，当前仅授权全人口临时评分与新盲审，不授权正式自动动作。

## 1. 工程边界

1. 正式采集数据库始终只读。`exclude` 只表示不进入派生分析发布，不得删除或覆盖源记录。
2. 模型输入只有按冻结规则生成的规范化文本。`platform_key` 只保留为来源谱系字段，不得进入特征、切分、阈值、审计配额或性能门。
   规范化从候选构建绑定的冻结采集快照重做：普通正文原样进入通用规则；Quill Delta JSON 仅拼接字符串 `insert`，忽略已声明的图片/截断嵌入和全部格式属性。损坏 Delta、非法操作、未知嵌入或无文本结构以稳定 reason code 失败关闭。识别依据正文结构，不读取平台。
3. 训练、阈值策略和推理是三个解耦阶段：训练产出不可变模型与校准器；阈值策略独立冻结；初始全量和未来新增批次调用同一冻结模型。
4. 唯一权威标签证据是 `final-nonduplicate-model-reference` CSV 与唯一配对的 `finalized` manifest，不复制到 `text_post_annotations`。
5. 人工处理结果优先于模型路由；任何自动动作都必须能追溯到模型、策略、输入批次和输出 manifest。
6. Issue #46 的新标签先评价旧模型：`score-frame`、Wave A 与 Wave B 全程 `fit_call_count=0`；只有评价证据封存后，研究者才可另行批准这些标签进入新模型训练。进入训练的标签不再是该新模型的独立评价证据。

Wave A 人工 CSV 固定命名为 `wave-a-tourism-relevance-annotation.csv`，并平铺在对应 artifact 根目录；`<wave_id>/` 继续保存同字节不可变原件、manifest 与私有映射。表格使用 UTF-8 BOM，固定列序为 `task_id`、`sample_run_id`、`normalized_model_text`、`tourism_label`。`sample_run_id` 必须逐行等于不可变 `wave_id`；人工只填写最后一列，导入器只接受 `related`、`unrelated`、`uncertain`，不要求 `reason_code` 或 `evidence_note`。源帖子身份、版本、平台、两个模型的身份与概率、抽样层、入选原因、纳入概率和分析权重只写入私有映射。任务生成和完成表导入均拒绝列漂移、批次混合、正文变化、缺行、额外行或非法标签。

## 2. 最终700条不重复建模参考集

现有完成 CSV、旧 manifest、重复候选 CSV、人工重复决定、候补队列和补充标注只作为生成谱系，不是并列权威源。生成器执行以下冻结步骤：

1. 只读校验旧700条文件哈希、样本框、数据库身份、旧派生文本和旧概率证据；再校验冻结源快照哈希，从源标题/正文生成新的规范化文本投影。旧派生文本只作输入谱系，不能成为最终模型字段；
2. 对700条执行 `700 × 699 / 2 = 244,650` 个无序对的字符3–5 gram TF-IDF余弦计算；`similarity >= 0.80` 只进入人工候选，低于阈值的人工明确关系可显式补入；
3. 精确规范哈希相同自动成组，其他边必须人工最终确认；确认边取传递闭包。组内旅游标签冲突必须由独立最终证据关闭；
4. 初始700条的每个确认重复分量按规范化文本完整度、稳定帖子身份决胜；样本框、平台和旅游标签均不参与代表选择，产生的500/200框缺口由后续候补调度恢复；
5. 去重缺口从同一冻结候选人口按固定种子生成全局候补队列。队列在读取候补标签前冻结，不设平台配额、不按平台排序；候补进入标注前与初始700条全部谱系成员（包括已排除成员）和同轮候补执行相同重复检查，并沿初始确认分量传播。仅由候补组成的确认重复分量沿用完整度与稳定身份规则；触及任一已有确认分量成员的候补属于“确认重复候补”，不得计数，也不重新抽样既有代表；
6. 结构不可用、确认重复、重复候选未确认或标签未最终确定者不计数，继续按冻结秩补充，直至概率/定向框分别为500/200。前轮已标候补若在新分量中不再是代表，其标签不得自动转移给新代表；新代表必须获得自身人工最终标签，只有显式标签冲突确认可以覆盖分量代表标签。

概率框补样不得继承被替换记录的纳入概率或权重。只有能够从冻结补样设计证明联合纳入概率时，才可记录新概率和倒数权重；当前无法证明时 manifest 必须使用 `unavailable_after_replacement` 和稳定 reason code，总体估计不得使用。

最终验证器要求 manifest `status=finalized`，CSV 恰好700条，空白与 `uncertain` 均为0，源身份和重复分量身份各700个，最终确认重复对为0，样本框为500/200，并明确概率估计状态。CSV 至少包含任务/源身份、规范化文本、标签、样本框、选择原因与秩、合法概率证据、证据来源和重复分量；manifest 绑定标签手册、候选构建、新旧规范化规则身份、冻结源快照、文本投影及输入、成员、重复决定、候补队列和输出哈希。验证与训练均强制使用当前冻结配置从无 WAL、SHM 或回滚日志旁文件的源快照重新投影，逐身份核对最终正文及投影哈希；通过后训练才使用最终 CSV 的模型文本，绝不退回旧派生库正文。

首次开发误差复核后，17条明确违反既有标签边界的记录由 `related` 更正为 `unrelated`，1条待仲裁的第一人称婚纱摄影体验维持 `related`。随后完成68条隐藏模型答案的一致性复核：模型矛盾目标修改12/34，匹配正确对照修改1/34；用户仲裁批准10条、否决3条。获批方向为9条 `related→unrelated` 和1条 `unrelated→related`。当前最终标签为371条 `related`、329条 `unrelated`；最终 CSV SHA-256 为 `3eb3ea233c05bf76b5bdda548701895c9551726d74b6f9742523e695aeac73fd`，配对 manifest SHA-256 为 `ccf8b741b4bafaaf3e223e3c05d6eb844850f3c82168903b18cd69b3475d87f1`，成员摘要为 `5db2fc151a24a7ea97343aae762d76c5780b0bea09307125b4610171a4e66439`。概率总体估计仍为 `unavailable_after_replacement`。

## 3. 稳定配置入口

稳定入口为 `configs/cleaning.yaml`；以下是该通用冻结配置的历史字段边界，其中 `status` 与阈值 `UNSET` 是安全默认值，不代表当前实验终局：

```yaml
status: "FRAMEWORK_FROZEN / REFERENCE_DEDUP_FINALIZED / THRESHOLD_PENDING"
label_guide_version: text-cleaning-v1.5
random_seed: 20260728

reference:
  contract: final-nonduplicate-model-reference
  target_count: 700
  probability_count: 500
  targeted_count: 200
  duplicate_analyzer: char
  duplicate_ngram_range: [3, 5]
  duplicate_similarity_threshold: 0.80
  duplicate_threshold_role: candidate_only
  replacement_scope: global
  replacement_platform_quota: false
  replacement_platform_sort: false
  probability_invalid_behavior: mark_unavailable

text:
  analyzer: char
  ngram_range: [2, 5]
  min_df: 2
  max_df: 0.995
  sublinear_tf: true
  classifier: linear_svm
  svm_c: 1.0
  class_weight: balanced
  calibration: sigmoid
  calibration_folds: 5

leakage:
  author: true
  exact_duplicate: true
  confirmed_near_duplicate: true

split:
  temporal_test_fraction: 0.20
  validation_fraction: 0.20
  scope: global

routing:
  T_keep: UNSET
  T_exclude: UNSET
  unset_behavior: manual_review

audit:
  sample_size: UNSET
  max_event_rate: UNSET
  upper_confidence_limit: UNSET
  recovery_gate: UNSET
  population: retained_global

artifacts:
  normalization_config: configs/cleaning-text-normalization-v1.yaml
  normalization_version_lock: text-normalization-v1+sha256:<64位摘要>
```

配置解析必须拒绝未知字段、缺失字段、非有限概率和不满足 `0 <= T_keep < T_exclude <= 1` 的策略。任一阈值或正式门仍为 `UNSET` 时，只允许输出概率并把所有未有人工作答的记录路由到人工处理，不允许自动保留或自动排除。

开发阶段的模型选择门与上述训练/路由配置分离，冻结于 `configs/cleaning-model-acceptance.yaml`。该策略绑定 baseline `9cd30922aabf7fb2e2ba42e5a0396cfd`、当前参考 CSV 与训练/验证/锁定测试成员摘要；策略 ID 为 `3640f3c06ba3b658a4ba15076bd0e199`，完整 SHA-256 为 `3640f3c06ba3b658a4ba15076bd0e1993ab8ab715af7ddcfe054852ee1673d63`。它只决定 challenger 是否有资格成为唯一验证候选，不改训练超参，不启用自动路由，也不替代尚未冻结的最终测试门。

## 4. 训练与校准契约

### 4.1 泄漏分组和测试冻结

作者、精确重复和最终确认的近重复关系通过传递闭包组成泄漏分量。全体参考记录按时间选择最后 20% 为测试候选，再扩展到候选连接的完整泄漏分量。扩展后的测试集一次冻结，最终比例允许高于 20%。剩余数据进行全局分组分层训练/验证切分；平台不参与任何名额分配。

测试集不得用于词表拟合、特征选择、超参数搜索、Sigmoid 校准、阈值选择或失败后的反复调参。训练输出必须保存测试成员 manifest 和跨集合泄漏检查结果。

### 4.2 折外校准

首个正式模型为字符 TF-IDF 加固定 `C=1.0`、`class_weight=balanced` 的线性 SVM。训练部分内部最多使用五折相同泄漏分组交叉验证；如分量结构不能支持五折，实现在不低于两折且每个拟合端、留出端都含双类的范围内下降折数，否则失败关闭。每条训练记录只获得其未参与拟合的一个折外决策分数；无正则一维 Sigmoid 校准器只以这些折外分数和训练标签拟合。随后以完整训练部分拟合最终向量器和 SVM，冻结向量器、分类器、校准器、规范化规则和依赖环境。该 baseline 不执行 C 搜索，避免在尚未定义模型选择门时引入隐式调参。

校准器输出 `p_unrelated`，其值必须为 `[0, 1]` 内有限数。训练运行包保存训练折外概率和验证集概率；验证报告包括总体 Precision、Recall、F1、PR-AUC、混淆矩阵、Brier score 和 log loss，其中 `0.5` 只用于诊断分类指标，不是路由阈值。训练阶段的测试集状态固定为 `locked_not_opened`，不生成测试概率或测试指标。阈值策略冻结后，测试报告再包括总体指标、阈值表现、自动覆盖率和时间切片，不生成平台分类指标。

### 4.3 不可变模型 artifact

模型运行包至少保存：

- `model_id`、artifact SHA-256 与创建时间；
- 参考 CSV、参考 manifest、源快照和成员 manifest 的哈希；
- 规范化规则、字符 TF-IDF、线性 SVM、Sigmoid 校准器及其参数；
- 泄漏分组、训练/验证/测试成员与随机种子；
- Git SHA、Python 与关键依赖版本；
- 总体测试输出和机器可读验收状态。

artifact 一旦冻结不得原地覆盖。新增记录的到来不触发 `fit`，也不替换 artifact。

### 4.4 阶段性 baseline 运行身份

当前有效开发 baseline 为 `9cd30922aabf7fb2e2ba42e5a0396cfd`，显式绑定：

- 代码身份：`e2dfea19ac80d8b4e34c7948bfcbe85538d65fa8`；
- finalized leakage build：`6048c9aba31e020cb2fae9afb0b676e2`；
- 参考 CSV / manifest：`3eb3ea23...ac73fd` / `ccf8b741...5d87f1`；
- 模型 / 校准器 SHA-256：`f0a9cdf6d519d3b95d8126937af9bf57444dbe49ce72dfa5ea275362acf25b42` / `58f264f5fb79f3453fa149bfd1cb74cb4787572f05b3648a199fb69795bf9da3`；
- 训练包 manifest SHA-256：`7b5daf5af0032fa7f7acefc6d7cad4faf7b068185adf0435e263e10efc21fa01`；
- 开发误差 artifact SHA-256：`c1ac646044c6c6310ba531a4c4ee64ac863173ce113baec05e9b941568013d32`。

切分计数为训练442、验证110、测试148；测试成员 manifest SHA-256 仍为 `e8cbcbc306ff5faedf9b94fcac9a88ff0e3885ab4d74ea99b12c63eb52721ae8`，与前两次 baseline 完全一致。运行包只含训练 OOF 与验证概率，`test_status=locked_not_opened`、`threshold_status=UNSET`、`platform_used=false`。训练 OOF 在0.5诊断分界下为396/442正确，unrelated F1 91.1%，PR-AUC 97.4%，Brier 0.0792，log loss 0.2697；验证集为95/110正确，unrelated Precision/Recall/F1 为85.1%/91.9%/88.4%，PR-AUC为96.8%，Brier为0.0902，log loss为0.2804。该0.5不是路由阈值，指标不构成模型验收通过。标签修改会触发开发集重新分层，新旧训练/验证成员不完全相同，因此指标改善只是描述性证据，不能当作标签修改的净因果效应。

### 4.5 challenger 实验边界

下一轮模型提升按以下顺序实施，任何一项都不得读取测试概率：

1. 标签一致性复核已完成：复核者只看文本与规则，34条强矛盾记录与34条按原标签、切分和长度匹配的随机预测正确对照被随机混合；两组结果分别统计，不用于外推全体错标率。用户明确批准的10条已由 `apply` 入口原位封存，3条否决结果在 manifest 中维持原标签；
2. 首轮只建立三类有限候选：现有字符 TF-IDF＋线性 SVM 的小网格、字符 binary count＋NB log-count ratio＋线性 SVM、同一字符 TF-IDF＋逻辑回归。标题/正文分通道、中文分词特征和 fastText 不进入首轮；
3. TF-IDF＋线性模型只比较 `ngram_range ∈ {(2,5),(3,5),(2,6)}`、`min_df ∈ {1,2}`、正则强度 `C ∈ {0.3,1,3}`；`max_df=0.995`、`sublinear_tf=true`、类别权重、文本规范化和随机种子保持冻结。候选选择在训练侧嵌套 `StratifiedGroupKFold` 中完成，外层与内层都使用同一 leakage component；
4. 当前验证集已被分析，只能作为次级开发稳健性检查。sparse 唯一候选的验证 ID `6d105b334df0234350a4b5c32358f7a9` 已封存为 `directionally_consistent`；验证 Accuracy 由86.36%升至88.18%，UGC误排仍为10条，无关误留由5条降至3条，log loss/PR-AUC/Brier 三项方向均改善。该结果不允许继续修改 sparse 网格；
5. 因 sparse 改善幅度有限且预计人工灰区仍大，已另行完成唯一 Qwen3-Embedding-4B 冻结语义 baseline。它不是依据验证结果扩 sparse 参数，而是独立模型族：固定公开权重 revision、单通道全文、任务说明、2048 token、L2 归一化、唯一逻辑回归头、随机种子、训练预算和依赖身份。运行 `14feebc04a7a61b8b97f959a998d14dc` 的 UGC 误排率改善2.09个百分点，但 log loss、PR-AUC 与 Brier 均恶化，故 `validation_status=not_allowed`；
6. Issue #45 在上述失败之后冻结三层改进树：第一层只复用已封存训练 embedding 做 MRL 维度与线性头 nested OOF；若失败，第二层做每个外层训练端内独立选择的 sparse＋Qwen logit 融合；若仍失败，第三层采用固定英文 instruction 与 head-tail 双视图重新编码。任一层通过既有训练验收门即停止扩展；
7. 比较同时报告两类错误、PR-AUC、Brier、log loss、风险—覆盖率与自动覆盖；不得仅按 Accuracy 排名，也不得在验收门冻结前决定生产模型。

如果模型族、特征、超参数搜索空间或模型选择门发生变化，必须先同步科研方案、工程方案、配置身份与运行契约，再开始拟合。旧 baseline 和开发证据保持历史可验证，不得覆盖。

候选计划已冻结于 `configs/cleaning-text-challenger.yaml`，计划 ID 为 `ad515917c735ce77ed5231f0fa25bd53`，完整 SHA-256 为 `ad515917c735ce77ed5231f0fa25bd536e8395115f22b9bf4e5035f4df199301`。C1/C2/C3 各18个候选，共54个。C2 的比率采用二值文档出现、`alpha=1` 平滑、类别内 L1 归一化和 `log(P(feature|unrelated)/P(feature|related))`，并在每个拟合折内部估计；C1/C2 的 SVM 由相应训练端分组 OOF margin 拟合 Sigmoid，C3 使用 `liblinear` 原生概率。任何候选先要求内层 `related→unrelated` 点估计不劣于同折 baseline，再按 log loss、PR-AUC 和稳定候选 ID 排序；无安全候选时回退 baseline anchor。

`cleaning_train_sparse_challenger.py` 已实现只读输入复核、5折外层/4折内层的分组选择、逐成员 paired outer OOF、54候选评分、唯一候选模型、component bootstrap 验收与不可变运行包。训练运行 `ce19406cd132e55b2eb00531f5cc4cd3` 的五个外层训练端及全训练内层选择均选中 TF-IDF＋LinearSVC（`ngram_range=(2,5)`、`min_df=2`、`C=3`），候选模型 ID 为 `1b68baa8bef99d6b9d75b7bf3226cfb4`。442条 paired OOF 上 Accuracy 同为393/442，UGC 误排31→30、无关误留18→19；log loss、PR-AUC、Brier 点差分别为−0.0112、+0.0014、−0.0023，七项训练门全部通过。运行包固定 `validation_status=pending_directional_check`、`test_status=locked_not_opened`、`threshold_status=UNSET`、`auto_cleaning_decisions_present=false`。

`cleaning_validate_sparse_challenger.py` 只接受上述训练侧已通过的唯一候选，并与 baseline 已封存的110条验证概率配对；当前成员为48条 `related`、62条 `unrelated`、86个 leakage components。计算模块只暴露 `predict_p_unrelated`，语法树测试拒绝 `fit`/`fit_transform`；CLI 不接受候选、超参数、测试或阈值。相同输入形成同一 `validation_id`，首次原子封存逐成员概率和聚合报告，后续只校验并复用，不再次预测。方向描述固定为 UGC 误排率/log loss/Brier 不升且 PR-AUC 不降；不新增统计显著性门，结果只允许 `directionally_consistent` 或 `mixed_or_reversed`，且始终 `may_expand_search=false`。

Qwen 语义 baseline 计划冻结于 `configs/cleaning-qwen-embedding-baseline.yaml`，计划 ID 为 `56a5900909834c0877725bf3367d295e`，完整 SHA-256 为 `56a5900909834c0877725bf3367d295ef5c94bc39527e7737bb5fe802b2b461a`。公开权重固定为 `Qwen/Qwen3-Embedding-4B` revision `5cf2132abc99cad020ac570b19d031efec650f2b`，14文件规范快照 SHA-256 为 `cce6e0f7cd81e6c7cf31a67708362e6e9762b6c343d9805506c08ee283d0bac9`，两片 safetensors 权重的规范聚合 SHA-256 为 `e49e59781ff5f117a16cbf9e37655202ce529729ace5aaf3b68fa88fe57906b9`。运行时逐文件校验完整快照，并要求权重索引只引用预登记的两片文件。编码器完全冻结，输入仍为单通道 `normalized_model_text`，统一任务说明不含标签示例或平台；最大2048 tokens、末 token pooling、2560维输出转float32后再次L2归一化。执行身份固定为 MPS、batch=1、参数 bfloat16，实际硬件、macOS/Darwin版本、Python 及包含 tokenizers/safetensors 的依赖版本进入 run ID。唯一线性头为 `LogisticRegression(C=1, class_weight=balanced, solver=liblinear)`，没有搜索空间。

`cleaning_prepare_qwen_embedding.py` 只处理公开权重，可在仓库外目录下载并逐文件校验固定 revision、两片权重和索引，用两条内置合成文本做冻结 MPS 烟雾测试。`cleaning_train_qwen_embedding_baseline.py` 只接受最终证据、切分锚点包、sparse comparator 包、本地模型目录和 artifact 根目录；CLI 不允许覆盖设备、batch、dtype 或代码版本，要求干净工作树及 `--execute-training`。入口只编码训练442条，执行固定候选 leakage-group OOF，把 sparse candidate 概率作为 baseline 做同成员/标签/component配对验收，再原子保存训练嵌入、含折号概率、截断统计、风险—覆盖率、原始计划/策略、线性头和 manifest。4B运行 `14feebc04a7a61b8b97f959a998d14dc`、模型 `cb5bad8cd27c2c5df9edea3a2ca20834` 和 manifest SHA-256 `2e4e8b34688e1bdd4d968bb4c56a22a17d19236a7076789c5c9aa7584712cbf1` 已不可变封存；442条中57条超过2048 tokens，最大70,077，验证与测试均未读取。

第一层配置为 `configs/cleaning-qwen-head-challenger.yaml`，计划 ID `f9a1cccab81b2ea9ba8c26b5d539781c`、完整 SHA-256 `f9a1cccab81b2ea9ba8c26b5d539781cbb54ba991e185d9a16f19323ae492fb9`；它只绑定上述 manifest、训练 embedding SHA-256 `0517a29e4a14ddadfaa36fc4f69d803227f84a2e746f4864bb641d1acfc423ec` 与训练 OOF SHA-256 `ae537f5648848ca94d4c5bcdc2617b9dde8a27f58480eb97f3218b3bc7c047af`。候选是4个 MRL 前缀维度乘以逻辑回归8种头和 LinearSVC 6种头，共56个；所有前缀重新 L2 归一化，所有头都由当前拟合端分组 OOF margin 拟合 Sigmoid。`cleaning_train_qwen_head_challenger.py` 不接受 CSV、数据库、正文、模型目录、验证、测试、平台、阈值或审计参数；它执行外5/内4 leakage-group nested OOF，内层以原4B头规范为安全锚，外层完成后才与 sparse 配对验收，并原子封存模型、逐成员概率、56候选评分、折内选择、计划、策略、报告和 manifest。外层选择禁止读取全局 sparse OOF，避免 comparator 的其他折训练谱系间接泄漏到外层选择。

第一层运行 `3d23bc1b9b8c2957856851fa1454eccd`、模型 `a2f919b09d1a78e7b8a8b749524013a0`、manifest SHA-256 `bf5936977f4712b556e2b0cb41cdb092330ebbb1c46e543bc58516970ae135ee` 已封存。全训练端选中2560维 `LinearSVC(C=1, class_weight=balanced)`＋OOF Sigmoid；相对 sparse，UGC误排率、log loss和Brier分别改善2.09个百分点、0.0333和0.0129，但 PR-AUC退化0.0142，高置信0.90诊断下真实UGC误排9条而 sparse 为4条，log loss差的90% bootstrap上界仍为+0.0013。因此运行固定 `acceptance_status=failed_retain_baseline`、`validation_status=not_allowed`，没有生成验证或测试概率；第二层不得只拿这份全局 OOF 直接拟合融合权重，必须在每个外层训练端内重建两个基模型的内层 OOF。

第二层计划位于 `configs/cleaning-qwen-sparse-fusion.yaml`，计划 ID `7198161453f248a70197a8a960506501`、完整 SHA-256 `7198161453f248a70197a8a9605065010a035787fe984551d11946524b33c94d`；同外层配对策略位于 `configs/cleaning-qwen-fusion-model-acceptance.yaml`，策略 SHA-256 `42b1770fe0241fb7aa69303878141330cedcd7cb54053ed82908d8fad64d7836`。`cleaning_train_qwen_sparse_fusion.py` 在每个外层训练端内重新拟合固定 sparse candidate `1ed77d39ddf8f7c8d75c98412d3eb073` 的字符向量器、SVM及分组 OOF Sigmoid，并重做第一层56候选 Qwen 头选择；随后只以内层 OOF在logit空间比较 Qwen 权重0、0.25、0.5、0.75、1。0与1是审计锚点，权重先满足不劣于同内层 sparse 的UGC误排率，再按log loss、PR-AUC和稳定ID选择。外层 sparse 与融合概率来自相同拟合端和相同留出端，故新策略明确 `paired_outer_folds=true`；sparse网格不重搜，验证、测试、平台、阈值和审计均不进入入口。

第二层运行 `eba8568816309688f1f85e6092a57b1d`、模型 `4eb42812a7d9d68388418a6d58442c2b`、manifest SHA-256 `37fcdc206ba4c7c43afe2820ad2c47292925a91a8bf191084dc13b9c8ed261d3` 已封存。全训练端权重为0.5，外层权重分布为0.5三折、0.75两折；重建 sparse OOF 与既有正式 sparse OOF 逐成员完全一致。融合把UGC误排率降低3.14个百分点，log loss与Brier改善0.0440和0.0144，且log loss差90%上界为−0.0217；但 PR-AUC退化0.005350，超过0.005点门，其90%下界−0.01543也超过−0.01区间门，固定0.90高置信UGC误排由4条增至8条。因此仍为 `failed_retain_baseline / validation_not_allowed`，不得因距点门仅0.000350而事后放宽策略。

第三层计划位于 `configs/cleaning-qwen-english-head-tail.yaml`，计划 ID `c1345c2efc9c7ec28fcb26ec160eeee6`、完整 SHA-256 `c1345c2efc9c7ec28fcb26ec160eeee64218cf902a72b5124ecb0d1797d0be04`。`cleaning_train_qwen_head_tail.py` 先验证第二层确为失败且未读验证，再复用同一4B公开权重和执行身份，将 instruction 固定为英文边界说明。tokenizer 从2048减去 prompt 与特殊 token 得到名义内容窗口预算；当前模型为2001 tokens。短文本编码一次；超限文本各取头部/尾部视图，每个视图在解码并与 prompt 拼接后以最终重分词长度二分回缩到不超2048的最大可行窗口，两个 L2 向量取算术平均后再归一化。中段是否省略以实际保留的源 token 数计算，不以2001的两倍作硬编码覆盖承诺。入口要求所有实际送入模型的视图不超2048，否则失败关闭；运行报告分别保存原输入超限、两视图、tokenizer边界回缩视图、实际保留 token、中段省略、累计省略 token 和三类长度分组 OOF，不保存正文或逐成员长度。首次正式启动已在 artifact 产生前暴露并拒绝了“解码窗口重分词后超限”的边界；修正后仍保留最终长度硬检查。新向量随后复用第一层56候选外5/内4 nested OOF，并以既有 sparse OOF 执行原Qwen验收门。

正式运行 `bdf73219d584edfcbeea02772716a90a` 封存模型 `e24fc7a65a5e0e52f383725e21607837`，运行包 manifest SHA-256 为 `e4736d350c4aa7ba5d1cbb8dc03f8c2565d30e31bc2e5389bdffd4dd0b1a625e`，绑定代码 `2b2ad3351e5794ceaa34168a1cdf99c87d3a796c`。运行把60条超限输入展开为120个视图，连同382条单视图共502个；29个视图发生边界回缩，最终编码视图超限为0，23条仍省略中段。全训练端选择2560维、`C=1`、无类别权重的 LinearSVC＋OOF Sigmoid。八道Qwen验收门中，UGC误排点估计及其区间、log loss点改善及其区间、PR-AUC点非劣和Brier点非劣六道通过；PR-AUC差的90%下界−0.01355越过−0.01门，固定0.90下高置信UGC误排由4条增至7条，故最终 `failed_retain_baseline / validation_not_allowed`，未读验证或测试。

Qwen 相对 sparse 的独立验收策略冻结于 `configs/cleaning-qwen-model-acceptance.yaml`，策略 SHA-256 为 `3d201246b06fa58f48f88083c81f0de16588da9520e30944c3bf5354ab06ff7a`。Qwen 固定候选 OOF 与 sparse nested OOF 绑定同一成员、标签和 component，但 `paired_outer_folds=false`，不虚构相同折号。策略自包含安全、log loss、PR-AUC、Brier、component bootstrap 及固定0.90高置信度 UGC 误排条数不劣门；完整置信度网格对两模型同时报告覆盖、选择性风险、两类错误和差值，固定0.1/0.9也同时比较中间带。所有这些仅作开发诊断；balanced 逻辑回归分数是开发样本条件概率诊断，不是候选人口后验概率。无论结果如何，`T_keep/T_exclude` 和审计策略仍为 `UNSET`。

标签一致性工具已实现为 `cleaning_blind_label_review.py prepare/summarize/apply`：生成阶段只读取训练 OOF 与验证概率，任务固定列被私有映射摘要绑定；回填阶段拒绝成员、文本或映射篡改，并分别汇总目标与对照；应用阶段要求用户逐 key 批准、显式执行开关和操作前 CSV 哈希，只修改获批标签。复核 ID `c5e4c7139c703e04309f80a78506e7ee` 的10条获批改动已应用，3条否决改动已显式记录；应用回执 SHA-256 为 `f0435f308426865a141095f42f89f55e19393b2306c1e28c039ad93a9c82340b`。

`cleaning_evaluate_model_acceptance.py` 实施冻结的“UGC 安全优先”顺序。所有输入必须绑定同一训练成员、标签和 leakage component，并由策略声明 `evidence_scope` 与 `paired_outer_folds`：sparse challenger 要求同一 nested 外层折，Qwen 固定候选相对 sparse comparator 则明确为非同折配对；两者都以 leakage component 为单位固定运行5,000次 bootstrap。候选必须满足策略声明的误排、log loss、PR-AUC 与 Brier 门；Qwen 策略另含0.90高置信度 UGC 尾部门。任一失败固定 `retain_baseline`，Accuracy 不能覆盖安全失败。0.5和0.90均仅用于模型安全诊断，不是路由阈值。

## 5. 独立阈值策略契约

人口评分计划、Wave A抽样计划和路由策略是三个不同身份。`configs/cleaning-model-reliability-study.yaml` 已绑定完成的人口评分与Wave A抽样，必须按原哈希保留；`configs/cleaning-model-routing-selection.yaml` 绑定已封存评分、Wave A、完成表与基础评价，并沿用标签打开前记录的双阈值网格。唯一策略与Wave B抽样规则由 `configs/cleaning-model-routing-policy.yaml` 独立绑定，不得原地改写旧计划。当前策略配置 ID 为 `30a0806c341546c749034a07597b8d9b`；最终测试与审计由 `configs/cleaning-model-deployment-acceptance.yaml` 另行绑定，计划 ID 为 `0244dc9359679616a20bede755ff1791`。后者冻结 Qwen `0.14/0.86`、Wave B 完成表与评价、148条测试身份、测试零方向性错误门和双尾审计。正式判读后 `deployment_status=MANUAL_ONLY`，该策略不能被正式推理入口当作部署许可。

阈值策略是与模型分离、不可变并可哈希的 artifact，至少保存：

- `policy_id` 和策略内容 SHA-256；
- 所绑定的 `model_id`；
- `T_keep`、`T_exclude`；
- Wave A评分、抽样、完成表和评价manifest哈希；
- 双阈值候选网格、相同工作量比较网格、Pareto规则和自动排序规则；
- Wave B三段动作交叉层、分层容量、样本分配、纳入概率和评价状态；
- 双尾审计的事件定义、样本量、事件门、单侧置信上限和区间方法；
- 锁定测试最低尾部支持、方向性错误门和审计失败后的恢复条件；
- 决策依据、冻结时间和适用批次边界。

调整阈值只能创建新 `policy_id`，不得重新训练模型、覆盖旧策略或改写已完成运行。策略必须满足：

```text
p_unrelated <= T_keep             -> auto_keep
T_keep < p_unrelated < T_exclude  -> manual_review
p_unrelated >= T_exclude          -> auto_exclude
```

边界等号不得由实现自行改变。`auto_exclude` 只作用于派生发布状态。

Wave A 标签打开前，策略候选配置必须冻结完整二维网格。评价入口复用10,103条封存概率，不重复调用模型；对每个模型、每组双阈值计算完整人口路由数量，并用Wave A设计权重估计两端错误。推荐逻辑先去除人工量不低且两端风险均不优的组合，再按UGC误删、自动处理量、保留端误留、不确定性和模型成本形成可读候选。只有研究者确认后才产生唯一 `policy_id`。

Wave B只接受一个已冻结 `policy_id`，按selected与comparator的 `auto_keep / manual_review / auto_exclude` 形成3×3交叉层。分层分配必须在读取人工标签前绑定人口层容量并保存纳入概率；评价完成后不得原地修改策略。哈希子目录封存 `annotation` 原件，人工工作副本以 `wave-b-tourism-relevance-completed.csv` 平铺在 artifact 根目录；若该副本已存在，复用运行不得覆盖。Wave B只产生 `WAVE_B_EVALUATION_COMPLETE` 或 `evidence_insufficient` 等证据状态，不自动授权正式推理。

## 6. 推理运行与预测契约

初始全量与未来新增批次共用独立推理入口。入口只允许加载一个已冻结模型和一个与其兼容的阈值策略，不得调用任何训练或校准方法。目标批次必须先完成结构门和同版规范化。

推理前以冻结身份集合排除所有已有明确人工决定的记录。当前最低集合是最终700条；Wave A/B 中已封存为 `related` 或 `unrelated` 的唯一成员也直接采用人工决定，`uncertain` 进入人工中间层。13,858条候选减最终700条后，当前初始待推理人口为13,158条；10,103条仅是独立评价人口，评价阶段隔离的3,055条其他记录必须回到正式推理范围。其余记录逐条保存：

- `inference_run_id`、`model_id`、`policy_id`、目标批次 ID；
- 冻结帖子身份与规范化输入哈希；
- `p_unrelated`；
- 原始路由动作与是否因降级状态转人工；
- 人工任务引用、最终决定来源和最终决定；
- 输入、预测和输出 manifest 哈希。

同一目标身份、模型和策略的重复运行应得到相同概率与路由。任何输入不兼容、模型哈希不符、策略未冻结或输出不完整都必须失败关闭。

## 7. 人工证据与最终决定

人工灰区、未来新增人工结果和保留集审计各自使用独立的任务 CSV、完成 CSV 与 manifest，不复用同一任务身份。人工结果覆盖模型建议：

- `related`：最终保留；
- `unrelated`：从派生分析发布中排除；
- `uncertain`：维持待处理，不进入正式发布。

人工标签仍由对应完成 CSV 与 manifest 保存。数据库只保存最终帖子决定，以及完成 CSV SHA-256、任务 ID、模型 ID、策略 ID、推理运行 ID 等证据引用；不得把 CSV 标签再批量复制到 `text_post_annotations`。源库始终只读。

## 8. 双尾审计与降级状态机

锁定测试通过后，从临时 `auto_keep` 与 `auto_exclude` 人口各做150条尾内简单随机无放回盲审，不设平台配额。保留端的 `unrelated/uncertain` 与排除端的 `related/uncertain` 分别是不利事件，每尾均要求0事件。0/150的单侧95% Clopper–Pearson上界为1.977%，只作为保守风险上界，不表示误差为0。

未来实现至少包含以下状态：

1. `policy_unset`：允许打分；所有未有人工作答的记录转人工。
2. `provisional_routing`：锁定测试通过后生成临时动作，只用于灰区和审计任务，不形成正式自动决定。
3. `automatic_routing_enabled`：两个尾部审计都通过，三个区间才按冻结策略形成正式决定。
4. `auto_keep_suspended` / `auto_exclude_suspended`：对应尾部出现任一不利事件后，该尾部全部转人工；另一尾部按自身状态处理。
5. `recovered_with_new_policy`：只有新策略身份和失败尾部的新独立审计通过后才能恢复；旧策略和旧运行保持不可变。

审计失败不得通过换 seed、删除不利样本、替换任务、追加样本或改写旧策略来消除。正式可靠性声明分别覆盖自动保留残留无关风险和自动排除UGC误删风险；未来新增批次仍须按冻结增量审计规则重新建立证据。

## 9. 失败语义

以下情况一律停止自动决定并写出机器可读失败原因：参考 CSV 或 manifest 哈希不符、身份或文本不一致、泄漏分组跨集合、测试集被用于调参或重复打开、模型或策略 artifact 不完整、概率非有限、阈值非法、推理尝试调用训练、人工结果冲突、源库出现写操作、审计未通过或失败后仍执行对应自动动作。

失败不删除中间证据，不覆盖既有运行。可恢复问题通过新的运行 ID 重试；改变阈值必须创建新策略，改变模型必须形成新的模型 artifact 和独立决策记录。

## 10. 实现顺序与验收

参考集生成和验证代码、独立内部 schema、训练/折外校准/不可变 baseline 运行包，以及标签一致性复核后的阶段性 baseline 已经完成。执行状态与后续顺序如下：

1. **已完成**：近重复人工最终确认与标签冲突关闭；
2. **已完成**：候补队列、候补重复复核、补充标注和唯一最终700条 CSV＋manifest；
3. **已完成**：包含作者边的 finalized leakage build；
4. **已完成**：首次 baseline、开发误差复核、17条明确标签更正及重训；
5. **已完成**：68条隐藏模型答案复核、10条批准修改、3条否决维持、证据重新封存及 baseline 重训；
6. **已完成**：用户确认 UGC 安全优先，数值模型验收门、配对 component bootstrap 与失败保留 baseline 规则已冻结并实现；
7. **已完成**：NB-SVM 细节、54候选矩阵、嵌套分组计算、不可变 artifact、可读 CLI 与训练侧 UGC 安全验收入口已冻结并实现；
8. **已完成**：预登记 sparse challenger 训练侧比较通过，唯一候选及 paired OOF 已不可变封存；
9. **已完成**：sparse 唯一候选的一次无拟合验证方向复核为 `directionally_consistent`，不得再扩 sparse 搜索；
10. **已完成**：Qwen3-Embedding-4B 固定语义 baseline 正式训练 OOF 已封存；安全点估计改善，但 log loss、PR-AUC 与 Brier 门失败，保留 sparse 且未读取验证；
11. **已完成**：Issue #45 第一层缓存 embedding 的56候选分类头 nested OOF 已封存；概率校准改善但排序与高置信UGC尾部门失败，验证未开启；
12. **已完成**：第二层同外层无泄漏融合已封存；安全、log loss和Brier改善，但PR-AUC与高置信UGC尾部门失败，验证未开启；
13. **已完成**：第三层英文 instruction＋head-tail 截断缓解、56候选复评、长度分组诊断与不可变 artifact；截断实现有效，但两道尾部验收门失败，Qwen验证未开启；
14. **已完成**：Issue #46 对10,103条独立评价人口执行两个冻结模型各一次纯预测并封存人口框；
15. **已完成**：Wave A 240条六层概率抽样、四列盲标、完成表校验和基础设计加权评价；
16. **已完成**：正式双阈值分析 `91e05fc4a20317a69d31d32150bd472f` 封存完整三段指标、相同目标人工率比较、证据警告和跨模型 Pareto 前沿；42个 Pareto 点全部属于Qwen；
17. **已完成**：研究者确认 Qwen `0.14/0.86`；等人工量 sparse comparator 为 `0.20/0.80`，新标签保持 evaluation-only；
18. **已完成**：策略manifest与 Wave B `aecfd402eace2041aa0276d27420dafd` 已封存；360条按3×3交叉层抽样，纳入概率与权重只保存在私有映射；
19. **已完成**：完成表 `6c0f24e…b891` 已保全，任务模板恢复原摘要；独立评价 `fc5c2721c4e15e8addf7c55ab115950e` 的证据状态为 `sufficient_for_descriptive_independent_evaluation`，不换模型或改阈值；
20. **后续阶段**：评价全部封存后，由研究者另行决定新标签是否进入训练；进入训练须创建新模型身份和新的独立验证边界；
21. **已完成**：锁定测试判读与双尾审计计划 `0244dc9359679616a20bede755ff1791` 及封存包 `723a8c59a3f6e2fa7d581dde1fe1c5fa` 已冻结；manifest SHA-256 为 `3400335ad5084dcf202d58fbdbb04f309d45b834814d095e4defb50efa77572d`；
22. **已完成**：锁定测试运行 `3ae5ccddabf10548f28e472bd4d46695` 已单次消费148条测试成员，manifest SHA-256 为 `eb61b5cdb44c85f49694c5998f5638a1e13ffd675c455fa12851cc96956b8b8d`；`fit_call_count=0`、`prediction_call_count=1`、`test_access_count=1`；
23. **终局状态**：Qwen `0.14/0.86` 路由为103条自动保留、41条人工、4条自动排除；保留端2个方向性错误、排除端1个方向性错误，且排除端支持低于20条。结果为 `FAILED_MANUAL_ONLY / MANUAL_ONLY`；部署审计保持 `FROZEN_NOT_RUN` 且不启动，未生成正式自动决定；
24. **后续边界**：可以把当前概率作为人工优先级辅助，但所有未有人工作答的记录必须人工处理。若另行批准 Wave A/B 标签进入新模型训练，必须创建新模型身份和新的独立评价/测试边界；已开启的148条不得再次充当最终测试。

验收必须首先证明最终700条是唯一权威证据、身份与标签完整、无最终确认重复、500/200明确且概率有效性不伪造；随后证明平台字段不进入阈值、配额、排序、模型、切分或性能门，作者 leakage 不跨集合，折外校准不接触测试集，源库只读。其余路由验收保持不变。

当前预冻结验收已经给出否定终局，因此不存在可执行的正式自动清洗流程。只有新的模型周期和新的独立评价边界获得另行批准并完成后，才可重新讨论 `CURRENT / FORMAL`。
