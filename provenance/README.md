# 研究谱系

`provenance/` 保存论文结论与数据、代码和实验之间的映射：

- `data/`：源库哈希、派生规则、行数和数据版本；
- `environment/`：Python、依赖、操作系统和硬件快照；
- `experiments/`：论文引用的正式 `run_id` 索引；
- `releases/`：数据、标注、模型、图表和论文版本的冻结记录。

谱系文件只保存元数据和哈希，不复制原始 UGC 或大型工件。
