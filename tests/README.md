# 测试策略

- `unit/`：文本规范化、去重、标签 schema、文本/视觉模型组件和指标函数。
- `integration/`：只读 SQLite → 清洗 → 标注导入 → 数据切分 → 预测的最小链路。
- `regression/`：固定小样本的行数、哈希、重复簇和指标不得意外变化。
- `fixtures/`：仅放合成或充分脱敏的小型测试样本。

测试不得连接平台、登录账号或真实抓取环境；不得依赖正式数据库才能完成基础单元测试。
